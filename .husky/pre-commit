#!/usr/bin/env sh
echo "▶ Running TypeScript check..."
NODE_OPTIONS="--max-old-space-size=4096" pnpm tsc --noEmit
if [ $? -ne 0 ]; then
  echo "✗ TypeScript check failed. Commit aborted."
  exit 1
fi
echo "✓ TypeScript clean."
