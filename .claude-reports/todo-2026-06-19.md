# Glev — To-Do für 2026-06-19 (Pre-Launch)

Stand: Donnerstag Abend 2026-06-18 nach intensivem Bugfix-Tag + Apple-Submission.

Apple Review läuft im Hintergrund (24-48h). Pipeline ist frei für die Pre-Launch-Polish-Welle.

Jeder Sprint-Prompt hat den Protected-Files-Header inline, copy-paste-ready für Replit/Claude Code.

---

## P0 — Apple-Re-Submission-Blocker & dringende Backend-Bugs

### P0 #1 — Macro-Rechner Regression + Aggregator-Reactivation + Source-Wiring

**Status:** Diagnose dfced7be done. TP1 fehlt → Aggregator wird bypassed. TP2 zeigt carbs=50, protein/fat/fiber=null. nutritionSource wird berechnet aber nicht ins SSE-Frame geschrieben.

````
🚨 PROTECTED FILES — NIEMALS ANFASSEN OHNE EXPLIZITE LUCAS-FREIGABE:
ios/App/App/Info.plist, PrivacyInfo.xcprivacy, App.entitlements, AppDelegate.swift,
ios/App/App.xcodeproj/project.pbxproj, capacitor.config.ts, capacitor.config.json,
DECISIONS.md (nur append), AGENTS.md (nur append),
package.json (nur explizit angefragte Dependencies), supabase/config.toml

🚨 PRE-COMMIT-PFLICHT: git status + git diff --stat.
🚨 BEI UNSICHERHEIT: STOPP > MUTIG ANGEHEN.

# SPRINT — Macro-Rechner Regression + Aggregator-Reactivation + Source-Wiring

## Findings aus dfced7be Diagnose
- TP2 trifft mit carbs=50, protein=null, fat=null, fiber=null
- TP1 fehlt → Aggregator-Pfad wird übersprungen
- nutritionSource wird vom Aggregator berechnet, aber nie ins meal_prep SSE-Frame geschrieben
- Voice-Pfad (/api/parse-food) funktioniert, AI-Chat-Pfad nicht

## Aufgabe 1 — Aggregator-Pfad in AI-Chat reaktivieren
In `app/api/ai/chat/route.ts`: parseFood/Aggregator-Aufruf VOR LLM-Tool-Call sicherstellen.
Pipeline:
1. AI Tool-Call requested mealMacros für "Pizza Margherita"
2. Backend: zuerst aggregator.aggregate(mealName) → DB-First (userMealHistory) → OFF → USDA → LLM
3. Result mit nutritionSource an LLM zurück
4. LLM packt das in pending_action mit ALLEN Feldern

## Aufgabe 2 — Tool-Schema in lib/ai/glevTools.ts: protein/fat/fiber required
Aktuell carbs_grams required, andere optional → LLM lässt null.
Fix: protein_grams, fat_grams, fiber_grams ALLE als required.

## Aufgabe 3 — nutritionSource ins meal_prep SSE-Frame
`app/api/ai/chat/route.ts`: pending_action SSE-Stream erweitern um nutritionSource.
`lib/useGlevAI.ts`: pending_action State Type erweitern.
`app/(protected)/engine/page.tsx`: nutritionSource auslesen + Source-Badge.

## Test
User-Input "Pizza Margherita" → Vercel-Logs:
- [DIAGNOSE-TP1] aggregatorCalled: source=user_history matches=5
- [DIAGNOSE-TP2] mealParams: carbs=75 protein=12 fat=15 fiber=3 source=user_history
Engine + Mini-Preview zeigt "Aus deinen Logs" Badge.

## Diagnose-Logs entfernen NACH Verify
[DIAGNOSE-TP1/TP2/TP3] console.logs raus nach End-to-End-Test.

## DECISIONS.md APPEND
| 2026-06-19 | Macro-Regression-Fix + Aggregator-Reactivation | [BUGFIX] AI-Chat-Pfad reaktiviert Aggregator. Tool-Schema macht protein/fat/fiber required. nutritionSource im SSE-Frame + Engine-Page-Render. Voice-Pfad unverändert. Diagnose-Logs entfernt. |

## Commit
fix(ai): reactivate aggregator + required macros schema + source badge wiring

## ABBRUCH
- protein/fat/fiber required-Markierung lässt Tool-Call fehlschlagen → STOPP, klären ob default=0 OK
- Aggregator-Bypass war bewusste Performance-Entscheidung → STOPP, Lucas klären
````

---

### P0 #2 — Sandbox-Test-Kauf

**Status:** wartet bis Apple-Pricing auf iPhone-Paywall sichtbar (5-15 Min nach Sub-Status auf „In Prüfung"). Kein Sprint nötig, manual workflow.

**Schritte:**
1. iPhone TestFlight Build 14 force-quit + reopen
2. Plan-Simulator auf Free → Paywall öffnen
3. Wenn echte Preise sichtbar: Pro Yearly tappen
4. Apple Buy-Sheet → „Andere Apple-ID" → Sandbox: `info@connectinglink-coaching.com`
5. Verify in Supabase: profiles.subscription_tier=pro, subscription_source=apple_iap
6. Verify in RC Dashboard: User als Customer mit aktivem Entitlement

---

### P0 #3 — Apple-Debug-Toast Cleanup

**Status:** 1-Liner Cleanup, kann zwischendurch.

````
🚨 PROTECTED FILES — NIEMALS ANFASSEN OHNE EXPLIZITE LUCAS-FREIGABE:
ios/App/App/Info.plist, PrivacyInfo.xcprivacy, App.entitlements, AppDelegate.swift,
ios/App/App.xcodeproj/project.pbxproj, capacitor.config.ts, capacitor.config.json,
DECISIONS.md (nur append), AGENTS.md (nur append),
package.json (nur explizit angefragte Dependencies), supabase/config.toml

🚨 PRE-COMMIT-PFLICHT: git status + git diff --stat.
🚨 BEI UNSICHERHEIT: STOPP > MUTIG ANGEHEN.

# SPRINT — Apple-Debug-Toast Revert zu generischem Text

## Kontext
Apple-Sign-In-Debug (3576d880) hatte den Login-Error-Toast auf Inline-Error 
"Apple-Debug: ..." gestellt für Diagnose. Bug ist behoben (216fd786 + 2f20fec6). 
Jetzt wieder generisch.

## Aufgabe
`app/login/page.tsx` handleAppleSignIn catch-Block:

statt:
  setError(`Apple-Debug: ${errorText}`);

setzen:
  setError(t.apple_error);

console.error bleibt für Logs.

## DECISIONS.md APPEND
| 2026-06-19 | Apple-Debug-Toast revert | [CLEANUP] Inline-Debug-Error wieder durch generischen t.apple_error Toast ersetzt. Bug-Fix-Diagnose abgeschlossen (3576d880 → 216fd786 → 2f20fec6). |

## Commit
chore(login): revert apple sign-in debug toast to generic message
````

---

### P0 #4 — CRM-Beautyflow-Email broken seit heute

**Status:** Diagnose-Sprint first, dann targeted Fix.

````
🚨 PROTECTED FILES — NIEMALS ANFASSEN OHNE EXPLIZITE LUCAS-FREIGABE:
ios/App/App/Info.plist, PrivacyInfo.xcprivacy, App.entitlements, AppDelegate.swift,
ios/App/App.xcodeproj/project.pbxproj, capacitor.config.ts, capacitor.config.json,
DECISIONS.md (nur append), AGENTS.md (nur append),
package.json (nur explizit angefragte Dependencies), supabase/config.toml

🚨 PRE-COMMIT-PFLICHT: git status + git diff --stat.
🚨 BEI UNSICHERHEIT: STOPP > MUTIG ANGEHEN.

# SPRINT — Diagnose: CRM-Signup-Notification an Beautyflow broken

## Symptom (Lucas 2026-06-18 Abend)
"Warum geht keine email raus an die neuen meta leads geht aber an beautyflow auf einmal nicht?"

CRM-Signup-Notification-Email an Beautyflow (Lead-Eingang im CRM bestätigen) 
geht seit heute nicht mehr raus.

## Diagnose — KEIN PRODUCTION-CODE-CHANGE

### A) Git-Log Heutige Commits
Letzte 24h Commits die `lib/emails/crm-signup-notification.ts`, 
`app/api/crm/signup-notification/route.ts`, `lib/emails/outbox.ts`, 
oder andere CRM/Email-Trigger touched.

### B) Resend Dashboard
Activity letzte 24h: Subject "Beautyflow" / "CRM" / Empfänger-Domain. 
Status: delivered / failed / pending?

### C) Supabase outbox
SELECT * FROM email_outbox WHERE created_at > '2026-06-18T00:00:00Z' 
AND (subject ILIKE '%signup%' OR template ILIKE '%crm%') 
ORDER BY created_at DESC.

### D) Vercel Function-Logs
/api/crm/signup-notification → Logs letzte 24h. Errors? Letzter erfolgreicher Call?

### E) Code-Trace
`app/api/crm/signup-notification/route.ts`: wie wird Email-Trigger aufgerufen? 
Auf welcher Bedingung wird er übersprungen?

### F) Findings-Doc
Output: `docs/crm-beautyflow-email-diagnose.md` mit:
- Heutige Commits (Verdachtsliste)
- Resend-Status
- outbox-Status
- Vercel-Function-Logs
- Code-Trace
- Hypothese welche Files den Bug verursachen
- Fix-Vorschlag

### G) Nichts pushen
Branch: `diagnose/crm-beautyflow-email` → Lucas review't, dann Fix-Sprint.

## DECISIONS.md APPEND
| 2026-06-19 | Diagnose CRM-Beautyflow-Email broken | [DIAGNOSE] Findings-Doc. Production-Code unverändert. Targeted Fix-Sprint danach. |

## Commit
docs(diagnose): crm beautyflow email outage

## ABBRUCH
- KEIN Production-Code-Change. Nur docs/ + Read-Only Tools/Logs.
````

---

## P1 — Show-Stopper UX/UI Bugs vor Launch

### P1 #5 — AI Chat öffnet 2 Mini-Previews bei kombinierter Eingabe

**Status:** Bug 1 Variante. fcc1b95f deckte nur Endlosschleife ab. Generation-Bug bei Multi-Item-Phrasen.

````
🚨 PROTECTED FILES — NIEMALS ANFASSEN OHNE EXPLIZITE LUCAS-FREIGABE:
ios/App/App/Info.plist, PrivacyInfo.xcprivacy, App.entitlements, AppDelegate.swift,
ios/App/App.xcodeproj/project.pbxproj, capacitor.config.ts, capacitor.config.json,
DECISIONS.md (nur append), AGENTS.md (nur append),
package.json (nur explizit angefragte Dependencies), supabase/config.toml

🚨 PRE-COMMIT-PFLICHT: git status + git diff --stat.
🚨 BEI UNSICHERHEIT: STOPP > MUTIG ANGEHEN.

# SPRINT — AI Chat: Doppel-Preview bei kombinierter Eingabe verhindern

## Symptom
User-Input "300g Bananen, 400g Erdbeeren, 20g Brot" → AI emittiert ZWEI 
pending_actions (Mahlzeit 1 mit Bananen+Erdbeeren+Brot, Mahlzeit 2 nur Lachs). 
AI fragt selbst "Soll ich die BEIDEN Mahlzeiten so speichern?".

## Findings (vermutet)
fcc1b95f-Fix (Bug 1) hat nur Endlosschleife (Re-Submit "Ich bestätige") 
abgedeckt + Supersede-Logic. AI splittet Eingabe in mehrere pending_actions 
wenn LLM Items als zwei Mahlzeiten interpretiert.

## Aufgabe
1. `lib/ai/glevChatPrompt.ts`: System-Prompt-Regel: "Auch wenn User mehrere 
   Lebensmittel in einer Eingabe nennt, IMMER als EINE Mahlzeit aggregieren. 
   Nur wenn User explizit 'zwei Mahlzeiten' / 'gleich zwei mal' sagt → 
   zwei pending_actions."
2. `lib/ai/glevTools.ts`: parseMeal-Tool darf nur EINE pending_action pro 
   User-Eingabe emittieren, außer explizit Multi-Meal-Intent.
3. Test: "300g Bananen, 400g Erdbeeren" → EINE Mini-Preview.

## DECISIONS.md APPEND
| 2026-06-19 | AI Chat Doppel-Preview Generation-Fix | [BUGFIX] Tool/Prompt aggregiert kombinierte Eingaben in eine pending_action. fcc1b95f-Fix war nur Endlosschleife. |

## Commit
fix(ai): aggregate combined meal inputs into single pending_action
````

---

### P1 #6 — Engine ↔ Chat-Fenster Navigation beliebig oft

**Status:** Bug 3 Variante. 8696e425 reicht nicht.

````
🚨 PROTECTED FILES — (Header oben paste'n)

# SPRINT — Engine ↔ Chat-Fenster Navigation beliebig oft hin/her

## Symptom
Nach Mini-Preview-Generation kann User zwischen Engine-Screen und Chat-Fenster 
nicht mehrmals hin/her springen. Beim 2. Wechsel zur Engine verschwindet 
Mini-Preview oder Re-Open-CTA wirkt nicht.

## Findings (vermutet)
8696e425 (Bug 3) hatte glev:meal-ai-saved Event + "Macros prüfen" Re-Open CTA. 
Aber: State wird irgendwo doch verworfen oder Event triggert zu früh.

## Aufgabe
Diagnose-First: lib/useGlevAI.ts → pending_action State-Lifecycle.

Test-Pfad: 
1. Voice/Text "400g Kartoffeln"
2. Engine öffnen 
3. Zurück zur Chat
4. "Macros prüfen" CTA → Engine wieder
5. Zurück zur Chat
6. Wieder Engine

Schritt 4-6: bleibt pending_action und CTA persistent?

Fix wenn State verworfen wird: pending_action erst auf `confirmed` setzen 
NACH expliziter Save-Aktion (glev:meal-ai-saved Listener), nicht bei jedem 
Engine-Open.

## DECISIONS.md APPEND
| 2026-06-19 | Engine↔Chat Navigation Refactor | [BUGFIX] pending_action persistiert über beliebig viele Engine↔Chat-Wechsel bis explizit gespeichert. |

## Commit
fix(ai): persist pending action across unlimited engine/chat round-trips
````

---

### P1 #7 — Bolus-Sheet X-Button unreachable

**Status:** Scroll-Bug. X versteckt sich.

````
🚨 PROTECTED FILES — (Header oben paste'n)

# SPRINT — Bolus-Sheet X-Button Scroll-Bug

## Symptom
Im Bolus-Detail-Sheet: User kann nicht so weit hoch scrollen dass das X 
ruhig erreichbar ist. Auto-Scroll triggert immer zurück → X verschwindet.

## Aufgabe
Datei: components/BolusDetailSheet.tsx (oder ähnlich — grep'en nach "X" + onClose).

Fix:
- Sticky-Header mit X immer fixed top (auch beim Scroll)
- Sticky position: top: env(safe-area-inset-top); z-index hoch
- Kein auto-scroll-Reset während User scrollt

## DECISIONS.md APPEND
| 2026-06-19 | Bolus-Sheet X-Button reachable | [BUGFIX] X als sticky-header, kein Auto-Scroll-Reset, immer erreichbar. |

## Commit
fix(bolus): sheet close button always reachable via sticky header
````

---

### P1 #8 — Voice-Transcribe-Error

**Status:** Diagnose-First. „Aufnahme konnte nicht verarbeitet werden" Toast nach Voice-Input. Files: lib/ai/sttRateLimiter, lib/ai/mistralClient, app/api/transcribe/*.

**Sprint-Prompt:** wird morgen direkt geschrieben wenn du da bist — Diagnose ist hier kontext-spezifisch (welcher Build, welche Phrase, welche Vercel-Logs).

---

### P1 #9 — Macros-Statistics-Widerspruch

**Status:** überlappt mit P0 #1. Sobald Macro-Pipeline + Source-Wiring sauber, sollte das Statistics-Widget auch konsistent zeigen. Pre-Check nach P0 #1.

---

### P1 #10 — Landscape-Glucose-Card rendert nur 1×

**Status:** State-Bug. Nach Portrait-Switch und zurück: kein Re-Render.

````
🚨 PROTECTED FILES — (Header oben paste'n)

# SPRINT — Landscape-Glucose-Card Re-Render-Bug

## Symptom
Glucose-Card im Querformat rendert beim ersten Mal korrekt. Nach Wechsel 
zu Hochformat und zurück zu Querformat: kein Re-Render mehr.

## Aufgabe
Vermutete Datei: components/LandscapeGlucoseOverlay.tsx (oder Layout.tsx 
Landscape-Branch).

Diagnose:
- useEffect Listener für Orientation-Change: cleanup unmounted oder 
  Component wird tatsächlich removed but State persists in falschem 
  Status?
- iOS-Specific WKWebView orientation event quirks?

Fix:
- Listener-Cleanup-Pattern prüfen
- Mount/Unmount-Cycle prüfen
- Falls iOS-Webkit-Quirk: orientationchange + resize zusammen abfangen

## DECISIONS.md APPEND
| 2026-06-19 | Landscape-Glucose Re-Render-Fix | [BUGFIX] Landscape-Component re-mountet zuverlässig bei jedem orientation-switch. |

## Commit
fix(landscape): glucose card re-renders on every orientation change
````

---

## P2 — Insights-Cluster Card-Polish (Pre-Launch)

Sammel-Sprint nach P0+P1. Alle 21 Insights-Karten Front+Backside Audit + Einzelfixes.

**Items:**
- 11: Swipe-Animation hakelig → Spring-Physics
- 12: Card-Höhen pro Cluster vereinheitlichen
- 13: Mahlzeitentyp-Erfolg-Card Layout + distinkte Chip-Farben + Wort-Wrap-Fix
- 14: Tageszeit-Erfolg-Card leerer Raum unten
- 15: Performance-Kacheln Flip-Layout-Standard
- 16: Adaptive Engine Card Backside kürzen
- 17: Mustererkennung-Card adaptiv (1 vs mehrere)
- 18: 21-Cards Audit als Sammel-Pass

**Sprint-Prompt** wird morgen kontextual geschrieben — die Card-Spezifika brauchen iPhone-Live-Reference, lohnt sich nicht jetzt blind.

---

## P3 — Sprache & Content

- 19: Carbs vs Kohlenhydrate auf Deutsch Konsistenz
- 20: Nightscout/Dexcom Tutorial via Loom
- 21: Founder-Videos
- 22: Mockup für Bekannte (Sidequest — Details kommen separat)

---

## P4 — Code-Refactor

### Bug 4 Legacy AI Rip-Out (jetzt risikoärmer)

Lucas hat klargestellt: Legacy = altes reines GPT-Pfade vor Mistral/Glev-AI-Chat. FAB triggert nicht mehr zu Legacy. Renders nur als Flash-of-Old-UI während Load. Heißt: dead code removal.

````
🚨 PROTECTED FILES — (Header oben paste'n)

# SPRINT — Legacy AI System Rip-Out (GPT-only-Pfade)

## Kontext
Legacy = altes reines GPT-AI-System vor Mistral/Glev-AI-Chat-Migration. 
FAB triggert nicht mehr dahin, User nutzen seit Mistral nur noch das neue 
System. Legacy rendert nur als Flash-of-Old-UI während App-Load.

## Phasen (eigener Branch `refactor/remove-legacy-ai`)

### Phase 1 — Diagnose & Markup
- grep -rn "classify-intent\|legacyVoice\|oldAI\|/api/ai/parse\|useOldVoice" 
  app/ lib/ components/
- Alle GPT-only-AI-Code-Pfade katalogisieren (Files + Funktionsnamen)
- /* TODO REMOVE: Legacy */ Kommentar als Marker setzen, KEIN Entfernen yet
- Output: docs/legacy-ai-removal-plan.md mit Removal-Order

### Phase 2 — Schrittweises Removal
- Pro markierten Pfad: entfernen, build, smoke-test Voice-Flow
- Jedes Removal eigener Commit für Rollback-Safety
- Tests anpassen oder löschen wo nötig

### Phase 3 — Cleanup
- Routing/Provider-Mounts/Auth-Hooks aufräumen
- classify-intent-Route nur entfernen falls Glev AI Chat sie wirklich 
  nicht braucht (vorsichtig — fcc1b95f hatte sie restored)

## DECISIONS.md APPEND
| 2026-06-19 | Legacy AI System Rip-Out | [REFACTOR] GPT-only-Pfade entfernt: ... (je nach Diagnose-Output). Glev AI Chat alleiniger AI-Pfad. Branch refactor/remove-legacy-ai. Flash-of-Legacy-UI verschwindet automatisch. |

## Commit (am Ende des Branches)
refactor(ai): remove legacy gpt-only voice/ai pathways

## ABBRUCH
- Phase 1 Diagnose vor Phase 2 → Pause + Output an Lucas
- Falls Pfade tief mit Auth/Sub verflochten: STOPP, Risiko-Klärung
````

---

### Insights Drill-in finales Polish

- Back-Nav in 73913895 ✓ verifizieren auf iPhone
- Layout-Tweaks falls Card-Höhen/Padding noch off

---

### Plus-Tier Eltern-Feature

Later. Plus-Tier ist live mit Sport-Insights + Arzt-Export. Eltern-Feature ist die nächste Erweiterung post-Launch.

---

## P5 — Backend / Operations

- 26: Email-Inbox-Sweep für sonstige Kundenfeedback (Connector oder manuell)
- 27: Customer-Email-Roadmap-Sweep aus 16.06. Reminder
- 28: Meta CAPI Marketer-Klärung am Wochenende
- 29: Supabase migrations tracking cleanup (140 Files, 0 applied)
- 30: CGM-Source ↔ Apple Health Activity Entkopplung

Jeweils eigene Sprints wenn angegangen — werden kontextuell geschrieben.

---

## P6 — Post-Launch (nicht morgen, für Memory)

- 31: iOS Widgets V1.1 Roadmap (Lock Screen / Live Activity / Home Screen / StandBy)
- 32: FPE-Anzeige (Macros-Screen, read-only Pill)
- 33: Splash + Loading Polish (Build 9+ UX-Sprint)
- 34: Android Test Pass (Push/Sound/DND-Bypass)
- 35: Dev Cockpit Phase 7 Test + Phase 8 Build mit GPT

---

## Bereits HEUTE abgeschlossen (zur Referenz)

- ✅ Apple IAP via RevenueCat Setup + Webhook Deploy
- ✅ Cross-Platform Entitlement Sync (Stripe + IAP)
- ✅ PaywallSheet UI + Plus-Tier Aktivierung (08bd418b, 7a58187d)
- ✅ AI Bug 1 (Endlosschleife) + DB-Cleanup (fcc1b95f)
- ✅ AI Bug 3 (Mini-Preview Lock) — 8696e425
- ✅ AI Bug 5 (Mistral-Prompt-Restore) — fcc1b95f
- ✅ AI Bug 6 (GlevAI Navigation) — 5d1287b0
- ✅ AI Bug 2 (DB-Anzapfung) — TEILWEISE: Voice-Pfad OK, AI-Chat-Pfad offen → P0 #1
- ✅ ExportPanel Plus-Paywall-Trigger — 3100c5c9
- ✅ Apple-Logo-Aspect-Fix — 66e04da1
- ✅ Apple Sign In Nonce-Hash-Fix — 2f20fec6 + 216fd786
- ✅ Insights Drill-in Pattern + Polish + Tier-Locks + KE/BE
- ✅ Apple-Submission ASC Version 1.0.2 Build 14 + 6 Subs „Warten auf Prüfung"

---

## Tomorrow-Start-Reihenfolge

1. Frühmorgens: Apple-Sub-Status checken
2. P0 #1 (Macro-Sprint) → größte Bug-Klasse weg
3. Parallel iPhone Sandbox-Test (P0 #2) sobald Apple-Pricing sichtbar
4. P0 #3 Cleanup nebenher
5. P0 #4 CRM-Email-Diagnose
6. P1 in Reihenfolge, P2 Sammel-Polish nach P1
