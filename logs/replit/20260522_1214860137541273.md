# Fix: Hardcoded English strings in expanded entry views (Entries screen)

**Task GID:** 1214860137541273  
**Datum:** 2026-05-22  
**Branch:** main

## Was wurde gemacht

Alle hardcodierten englischen Strings in den aufgeklappten Eintragsansichten der Entries-Seite wurden durch i18n-Aufrufe ersetzt. Deutsch und Englisch zeigen jetzt korrekt die jeweilige Sprache.

### `lib/exerciseEval.ts`
- `patternNote()`, `interimMessage()`, `finalMessage()`, `personalPatternHeadline()` — neuer `locale`-Parameter (default `"de"`), DE/EN-Inline-Übersetzungen für alle zurückgegebenen Strings.

### `lib/insulinEval.ts`
- `bolusInterimMessage()`, `bolusFinalMessage()` — gleiche Erweiterung mit `locale`-Param.

### `messages/de.json` + `messages/en.json`
- 23 neue Keys im `entriesExpand`-Namespace: `ex_post_workout_check`, `ex_1h_outcome`, `ex_1h_check`, `ex_2h_outcome`, `ex_notes_label`, `ex_hypo_risk_share`, `ex_disclaimer`, `basal_disclaimer`, `ex_waiting_at_end`, `ex_waiting_1h`, `ex_waiting_2h`, `ex_based_on_last_one`, `ex_based_on_last_many`, `ex_median_before_at_end`, `ex_median_before_1h`, `ex_n_need`, `ex_hypo_share_text`, `ex_n_classified_need`.

### `app/(protected)/entries/page.tsx`
- `ExerciseRowCard`: EVALUATION, POST-WORKOUT CHECK, 1H/2H OUTCOME, PATTERN NOTE, PERSONAL PATTERN, HYPO RISK SHARE, NOTES, Disclaimer, Warte-Nachrichten — alle via `tx()`.
- `InsulinLogRowCard` (Bolus): NOTES, Bolus-Disclaimer via `tx()`.
- `BasalRowCard`: Basal-Disclaimer via `tx()`.
- `InfluenceRowCard`: NOTES via `tx()`.
- `SymptomRowCard`: NOTES via `tx()`.
- `InfluenceEditor`: `tx` hinzugefügt, NOTES-EditorField übersetzt.
- `SymptomEditor`: `tx` hinzugefügt, NOTES-EditorField übersetzt.
- Import-Cleanup: ungenutzter `exerciseTypeLabel`-Import entfernt.

## Ergebnis
- App kompiliert sauber, Fast Refresh ohne Fehler.
- Kein neuer D-XXX-Eintrag nötig (keine Infrastruktur-, Schema- oder Auth-Änderung).
