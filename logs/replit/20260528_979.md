# Task #979 — Theme-Toggle aus Header entfernen & Light-Mode-Text fixen

## Summary

Der Dark/Light-Mode-Toggle sollte aus der App-UI und der öffentlichen Website entfernt werden. Theme-System bleibt erhalten, User-seitiger Toggle ist nicht mehr sichtbar.

Task-Agent-Branch konnte wegen persistentem Merge-Lock nicht applien — direkt im Main-Branch implementiert.

## Changes

**`components/Layout.tsx`**
- Import `ThemeToggle from "@/components/ThemeToggle"` entfernt
- `<ThemeToggle style={{ width: "100%", … }} />` Block aus dem Sidebar-Footer entfernt (war zwischen Nav-Items und Sign-Out-Button)

**`app/page.tsx`**
- Import `ThemeToggle from "@/components/ThemeToggle"` entfernt
- `<ThemeToggle />` aus dem Public-Marketing-Nav-Bar entfernt

**Light-Mode-Scan:** Alle `color: white` / `color: "#fff"` Instanzen in den Haupt-Screens (Dashboard, Engine, Entries, Insights, Landing Page) sind Button-Text auf Accent-Hintergrund (#4F6EF7) — korrekt, kein Fix erforderlich.

Das Theming-System (`ThemeProvider`, `lib/theme.ts`, CSS-Variablen in `globals.css`) bleibt vollständig erhalten — Systemtheme-Präferenz funktioniert weiterhin.
