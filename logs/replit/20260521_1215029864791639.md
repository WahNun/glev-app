# Adapt Score: Rename + Formel-Fix

**Task-GID:** 1215029864791639
**Datum:** 2026-05-22

## Was wurde gemacht

### 1. Formel-Fix — `lib/controlScore.ts`
- Nenner auf **ausgewertete Mahlzeiten** (GOOD + SPIKE + HYPO) beschränkt
- Ausstehende / OTHER-Einträge gehen nicht mehr in die Raten-Berechnung ein
- `score: number | null` — gibt `null` zurück wenn < 3 ausgewertete Mahlzeiten
- Aktive Nutzer werden nicht mehr durch viele ausstehende Einträge bestraft

### 2. Dashboard `ControlScoreCard`
- `hasScore = score !== null` für numerische Score-Anzeige
- `hasEntries = count > 0` für Badge + Aufschlüsselung
- Header: `adapt_score_title` ("Adapt Score") + `adapt_score_sublabel` ("Bolus-Präzision · 7T")
- Progress-Bar + `/ 100`-Label nur bei `hasScore`
- Bei null: `—` in ghost-Farbe, Badge zeigt "IM AUFBAU"

### 3. i18n (de.json + en.json)
- 3 neue Keys: `adapt_score_title`, `adapt_score_sublabel`, `adapt_score_tooltip`
- Umbenannt: `control_score_label`, `cluster_score_trend`, `pricing_pro_b3`

### 4. Display-Texte vollständig umbenannt
- `app/mockups/dark-cockpit/page.tsx` — 4 Stellen (StatCard label, STAT_COLORS key, STAT_INFO key, mobile dashboard array)
- `components/AppMockupPhone.tsx` — tabCaption DE + EN, Karten-Key via i18n automatisch
