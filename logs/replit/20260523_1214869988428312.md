# Task #1214869988428312 — Alle Insights-Karten einheitlich im Cockpit-Stil (#330)

## Summary
Alle Insights-Karten (außer `adaptive-engine`) erhalten eine Mindesthöhe von 460 px (≈57 % eines 852 px iPhone-Bildschirms). Sparse Cards bekommen kontextuelle Micro-Charts, damit der Platz sinnvoll genutzt wird.

## Changes

### app/(protected)/insights/page.tsx
- `FlipCard`-Komponente um `minHeight?: number`-Prop erweitert; Ghost-Div setzt `minHeight: minHeight || undefined`.
- `CARD_MIN_H = 460` als Konstante vor dem `items`-Array eingeführt.
- `minHeight={CARD_MIN_H}` auf alle FlipCard-Aufrufe angewendet (16 Stellen) außer `adaptive-engine`.
- Zwei-Spalten-Karten (`gmi-a1c`, `performance-tiles`) erhalten `minHeight: CARD_MIN_H` auf dem äußeren Grid-Div.
- `Sparkline`-Komponente um optionalen `height`-Prop erweitert (Standard 36); `glucose-trend`-Sparkline auf 100 px erhöht.
- Neuer `InsightMicroBars`-Helper (nach `Sparkline`-Funktion) für gleichartige Bar-Chart-Darstellung.
- **hypo-events** & **hyper-events**: 7-Tage-Tagesbalken (Readings unter/über Schwelle aus `readings14`).
- **glucose-variability**: 14-Tage-Tages-CV%-Balken (inline aus `readings14`).
- **meal-evaluation**: 7-Tage-Trefferquoten-Balken (Gut-Anteil pro Tag aus `meals`).
- **tdd**: 7-Tage-Tagesdosis-Balken (aus `tddByDay`-Map).

### messages/de.json + messages/en.json
- 4 neue i18n-Keys unter `insights`: `micro_trend_7d`, `micro_trend_14d`, `micro_hit_rate_7d`, `micro_tdd_7d`.

## Test Results
- TypeScript: `npx tsc --noEmit` — keine Fehler.
- engine-unit: 89/94 Tests grün; 5 Fehlschläge pre-existing (computeAdaptiveICR, applyAdjustmentToSettings, computeControlScore) — nicht durch diese Änderungen verursacht.
- App läuft stabil auf Port 5000.

## D-XXX Assessment
Kein neuer D-XXX-Eintrag nötig: keine Infrastruktur-, Schema-, Auth- oder Compliance-Änderung.
