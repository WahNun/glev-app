# Task #1238 — P3 User-Friendly Error Messages (Foundation Layer)

## Summary

Implemented a centralized error-mapping layer for the Glev AI chat system. All raw error strings are replaced with structured, user-friendly messages.

## Changes

### New files
- `lib/ai/errors.ts` — 9 `AppErrorCode` values, `ERROR_MESSAGES` map (DE/EN), `AppError` class, `RETRY_ALLOWED_CODES`
- `lib/ai/errorMessages.ts` — `getUserFriendlyMessage(code, locale)` + `isRetryAllowed(code)`
- `lib/api/errorResponse.ts` — `errorResponse(code, status)` server helper → `{ error_code, user_message, retry_allowed }`
- `tests/unit/errorMessages.test.ts` — 54 unit tests (all green)

### Modified files
- `app/api/ai/chat/route.ts` — 7 error paths migrated to `errorResponse()`, stream-catch emits structured SSE frame
- `lib/useGlevAI.ts` — client reads `error_code`, uses `getUserFriendlyMessage`; `GlevChatMessage.retryAllowed` added
- `components/GlevAIChatSheet.tsx` — conditional retry button for transient errors
- `messages/de.json` + `messages/en.json` — `"error_code"` section with all 9 codes
- `tests/unit/aiChatFeatureFlag.test.ts` — updated to check `error_code: "PERMISSION_DENIED"`
- `tests/unit/aiChatConsentGate.test.ts` — updated to check `error_code: "PERMISSION_DENIED"`
- `DECISIONS.md` — D-026 entry + Fix Log row

## Test Results
- 56/56 tests pass (errorMessages + aiChatFeatureFlag + aiChatConsentGate)
- Pre-existing smsOptOutGuard failures unrelated to this task
