# Session Report — 2026-06-02 (Task #1035)

## Aufgabe
Echtes Sprachtempo-Control aktivieren, sobald Voxtral es unterstützt.

## Ergebnis
Mistral bestätigte per Support-Anfrage, dass voxtral-mini-tts-2603 keinen nativen `speed`/`rate`-Parameter hat und auch keine Prompt-basierte Steuerung dokumentiert ist. Lösung zweischichtig implementiert:

## Erledigte Änderungen

### `hooks/useTTS.ts`
- `speedToFloat()` helper: `"slow"` → 0.75, `"normal"` → 1.0, `"fast"` → 1.3
- `audio.playbackRate = speedToFloat(speedRef.current)` vor `audio.play()` — wirkt auf das fertige MP3 client-seitig
- Web Speech API fallback: `u.rate` war hardcoded `1.05`; jetzt ebenfalls `speedToFloat(speedRef.current)`

### `app/api/tts/mistral/route.ts`
- `VOXTRAL_SPEED_TODO`-Kommentarblock + `const _speed`-Variable entfernt
- Tempo-spezifischer Style-Prefix je nach Speed:
  - `"slow"`: "...in langsamem Tempo mit kurzen Pausen..."
  - `"fast"`: "...in flottem, zügigem Tempo, fließend und energisch..."
  - `"normal"`: unveränderter Original-Prefix

### `docs/VOICE_ARCHITECTURE.md`
- Speed-Invariante beschreibt jetzt den `playbackRate`-Ansatz inkl. Mapping-Werte
- `VOXTRAL_SPEED_TODO`-Referenzen aus Invariants-Liste und Related-Decisions-Sektion entfernt

### `DECISIONS.md`
- Fix-Log-Eintrag für Task #1035 hinzugefügt

## Kein neuer D-XXX-Eintrag nötig
Kein Infra-, Schema-, Auth- oder Compliance-Change.
