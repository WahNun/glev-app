# Fix Report — ICR Comparison UI in Engine Step 2
**Task GID:** 1214984197854617
**Date:** 2026-05-20

## Was wurde gebaut

ICR-Vergleichs-UI in Engine Step 2 — zwei Selector-Karten (Adaptiv vs. Einstellungen), Adaptiv vorausgewählt, `effectiveICR` wird für die Bolus-Berechnung verwendet.

## Änderungen

### `app/(protected)/engine/page.tsx`
- **Neuer State:** `selectedICR: 'static' | 'adaptive'` (default `'adaptive'`)
- **Neues `useMemo`:** `staticICR` + `staticICRWindowLabel` — liest `getInsulinSettings().icr` und wendet `getEffectiveICR(at, base)` für das aktive Zeitfenster an; aktualisiert sich wenn `mealTime` oder `adjustmentTick` sich ändert
- **`effectiveICR`** = `selectedICR === 'static' ? staticICR : adaptedICR` — ersetzt `adaptedICR` in beiden `runGlevEngine`-Aufrufen (Step-2-`handleRun` + Step-3-Decision-Rec)
- **ICR-Vergleichskarten UI** direkt über dem Three-path-action-row in Step 2:
  - Bedingung: `icrSampleSize >= 3 && |adaptedICR - staticICR| > 0.5`
  - Adaptiv-Karte: zeigt `1 : {adaptedICR}`, Label "berechnet"
  - Einstellungen-Karte: zeigt `1 : {staticICR}` + optionalen Zeitfensternamen
  - Aktive Karte: ACCENT-Border + `${ACCENT}14` Hintergrund
  - Edge Cases: `icrSampleSize < 3` → keine Karten (Engine nutzt staticICR stillschweigend); Differenz ≤ 0.5 → keine Karten; `staticICR <= 0` → Hinweistext statt Karte

### `messages/de.json` + `messages/en.json`
Neue Engine-Keys: `icr_comparison_title`, `icr_static_label`, `icr_adaptive_label`, `icr_calculated`, `icr_select_button`, `icr_selected`, `icr_no_static_configured`

## Kein DB-Migration nötig
Reine Frontend-Änderung — kein neues Schema.

## TypeScript
Sauber — `npx tsc --noEmit` ohne Fehler.
