# Task #748 — MealNodeCluster badge color toHaveScreenshot baseline tests

## Summary
Added two Playwright test blocks to `tests/e2e/meal-node-bg-at-check.spec.ts`:

1. **Attribute test** — seeds a meal + `meal_timeline_checks` row (post_1, bg_at_check=112), mocks Supabase REST and `/api/cgm/history`, asserts `data-bg-at-check="112"`, badge text, and pre-knob absence.

2. **Screenshot tests** — three scenarios (bg=69 red, bg=112 green, bg=200 red-hyper) using `page.toHaveScreenshot` with a stable 40×10 px center crop. Stabilisation: JS injection sets badge `opacity="1"` and adds a solid white background rect so pixels are purely fill-colour-dependent regardless of chart position.

## Bug fixes
- `MealNodeCluster.tsx`: added `bgAtCheckKey` to `useEffect` deps (arms were not resyncing when `bgAtCheck` changed from null)
- `CurrentDayGlucoseCard.tsx`: `forceMealNodes`/`effectiveShowMealNodes` localStorage escape hatch for tests

## Baselines committed
`tests/e2e/meal-node-bg-at-check.spec.ts-snapshots/badge-color-{red-hypo,green,red-hyper}-chromium-linux.png`
