# Fix Report — Task 685
## Add automated tests to verify the AI chat receives real glucose and IOB data on the Dashboard

**Datum:** 2026-05-27

## Was wurde gemacht

### Neue Datei: `lib/screenContext.ts`
Alle reinen (React-freien) Hilfsfunktionen aus `hooks/useScreenContext.ts` in ein eigenständiges `lib/`-Modul extrahiert — ohne `next/navigation`-Import, vollständig unit-testbar:
- `pathToScreen(pathname)` → `GlevScreen` (alle 6 Werte)
- `resolveWants(screen, profile)` → `{ wantsGlucose, wantsIOB, wantsMeal }` (Consent-Gating-Logik)
- `trendArrow()`, `minutesAgo()` (reine Hilfsfunktionen)
- `getIOBSummary(sb, userId)` — injizierbarer Supabase-Client
- `getLastMealSummary(sb, userId)` — injizierbarer Supabase-Client
- Typ `InsulinLogRow` für Tests

### Refactoring: `hooks/useScreenContext.ts`
Importiert jetzt alle Logik aus `lib/screenContext.ts` statt sie zu duplizieren. Der inline Consent-Block (`glucoseConsented`/`iobConsented`) wurde durch den `resolveWants()`-Aufruf ersetzt. Re-exportiert `GlevScreen`, `ScreenContext`, `pathToScreen`, `resolveWants` für Downstream-Consumer.

### Neue Testdatei: `tests/unit/screenContext.test.ts` (25 Tests)
1. **`pathToScreen()` — 8 Tests**: alle 6 `GlevScreen`-Werte (dashboard, engine, entries, insights, settings, unknown), Sub-Pfad `/dashboard/settings` → `dashboard`, Root `/` → `unknown`
2. **`resolveWants()` — 9 Consent-Gating-Tests**:
   - `wantsGlucose=false` wenn `ai_consent_glucose_at=null`
   - `wantsGlucose=true` nur auf dashboard + Consent
   - `wantsIOB` auf dashboard+engine, nicht auf entries
   - `wantsMeal` auf dashboard+entries ohne Consent-Pflicht
3. **`getIOBSummary()` — 4 IOB-Fallback-Tests**:
   - `"Kein aktiver IOB"` bei leeren Logs
   - `"Kein aktiver IOB"` bei `null`-Logs
   - `"Kein aktiver IOB"` bei vollständig abgeklungenem Bolus (200 min, > DIA 180 min)
   - Formatierter `"≈ X.X IE aktiv (letzter Bolus gerade eben)"` bei frischem 8 IE-Bolus (5 min ago)
4. **NEUTRAL-Fallback — 3 Tests**: verifiziert `"Keine Daten verfügbar"` aus `useGlevAI.__test__.NEUTRAL` als Sentinel-Wert für fehlende `glucoseSummary`/`iobSummary` im AI-Request-Body

## Testergebnis
944 Unit-Tests laufen grün (vorher: 912). Kein neuer D-XXX-Eintrag nötig (keine Infrastruktur-/Schema-/Auth-/Compliance-Änderung).
