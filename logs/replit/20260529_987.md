# Fix Report — Task 987
## Refer-a-Friend System

**Datum:** 2026-05-29

## Was wurde gemacht

### Datenbank (Migration `supabase/migrations/20260529_referral_system.sql`)
- Neue Tabelle `referrals` (id, referrer_user_id, referred_user_id, referral_code, status, rewarded_at, created_at)
- Neue Spalten `profiles.referral_code TEXT UNIQUE` + `profiles.signup_source TEXT`
- RLS aktiviert, keine User-facing Policies

### Backend
- **`lib/referral.ts`**: `generateReferralCode()`, `getOrCreateReferralCode()`, `getReferralStats()`
- **`app/join/route.ts`**: GET `/join?ref=CODE` → setzt `glev_ref`-Cookie → redirect `/signup`
- **`app/api/me/referral/route.ts`**: GET → shareUrl, referralCode, referredCount, rewardedCount
- **`app/api/auth/signup-source/route.ts`**: POST → schreibt signup_source + referrals-Zeile
- **`app/api/checkout/pro/route.ts`** + **`app/api/checkout/plus/route.ts`**: referred users → Coupon `glev_referral_50` + kein Trial
- **`app/api/pro/webhook/route.ts`**: `rewardReferrerIfEligible()` bei `payment_status==="paid"` → +30 Tage für Referrer

### Auth Flow
- **`app/(auth)/signup/page.tsx`**: liest `glev_ref`-Cookie nach Registrierung, ruft `/api/auth/signup-source` auf
- **`app/(auth)/callback/page.tsx`**: gleiche Logik für OAuth-Callbacks

### Frontend
- **`app/(protected)/settings/konto/page.tsx`**: "Freund einladen"-SettingsRow (grünes Icon), Capacitor Share → Web Share → Clipboard-Fallback, Zählerchip
- **`messages/de.json`** + **`messages/en.json`**: `row_referral`, `subtitle_referral`, `referral_share_title/text/copy_success`, `referral_referred/rewarded_count`
- **`package.json`**: `@capacitor/share ^6.0.2` + `pnpm install`

### Decisions
- **D-031** in DECISIONS.md dokumentiert

## Checkliste
- [x] Migration `20260529_referral_system.sql` erstellt (manuell in Supabase Dashboard anzuwenden)
- [x] `lib/referral.ts` mit Code-Generator + Stats
- [x] `/join` Route mit Cookie-Setzung
- [x] `/api/me/referral` GET Endpoint
- [x] `/api/auth/signup-source` POST Endpoint
- [x] Signup + Callback lesen glev_ref Cookie
- [x] Checkout Pro + Plus: Coupon + kein Trial für referred users
- [x] Webhook: rewardReferrerIfEligible bei erstem Payment
- [x] Settings/Konto: "Freund einladen"-Zeile mit Share-Sheet + Fallbacks
- [x] Übersetzungen DE + EN
- [x] @capacitor/share installiert
- [x] D-031 + Fix Log in DECISIONS.md

## Offene manuelle Schritte (nach Merge)
1. **Supabase Dashboard → SQL Editor**: `20260529_referral_system.sql` ausführen
2. **Stripe Dashboard**: Coupon `glev_referral_50` erstellen (50 % off, once, kein Ablaufdatum)
