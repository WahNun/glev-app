# Fix Report — Task #555: Make personal insulin duration (DIA) configurable in settings

**Datum:** 2026-05-22  
**Task:** Configurable DIA (Duration of Insulin Action) in user settings

## Was wurde gemacht

### 1. DB-Migration
- `supabase/migrations/20260522_add_dia_minutes.sql` — neue Spalte `dia_minutes INTEGER CHECK (dia_minutes >= 60 AND dia_minutes <= 360)` nullable in `user_settings`

### 2. lib/iob.ts
- `getDIAMinutes(insulinType, userDiaMinutes?)` — akzeptiert optionalen User-Override; fällt auf typ-basierten Default zurück wenn NULL/NaN/< 60
- `calcTotalIOB(doses, insulinType, now?, userDiaMinutes?)` — neuer 4. Parameter übergibt User-DIA an alle Einzelberechnungen

### 3. lib/userSettings.ts
- `InsulinSettings`-Interface: `diaMinutes: number` ergänzt
- `DEFAULT_INSULIN_SETTINGS`: `diaMinutes: 180`
- `warnedField`-Latch: `diaMinutes: false` ergänzt
- `getInsulinSettings()`: liest + validiert `diaMinutes` aus localStorage
- `fetchInsulinSettings()`: SELECTs `dia_minutes` aus DB
- `saveInsulinSettings()`: upsert `dia_minutes` (clamp 60–360)

### 4. app/(protected)/settings/page.tsx
- `Settings`-Interface + `DEFAULTS`: `diaMinutes` ergänzt
- `SheetKey`-Union: `"dia"` hinzugefügt
- `upd()`-Guard: `diaMinutes` als "user-touched"-Feld eingetragen
- `fetchInsulinSettings`-Merge: `diaMinutes` aus DB übernommen
- `saveInsulinAction`: `diaMinutes` mit Clamp 60–360 in `clamped` eingetragen
- `diaSub`-Subtitle: `"{minutes} min"` aus i18n
- Neues Sheet `dia`: Erklärungstext + Zahlen-Input (60–360, step 15) + `SaveFooter`
- Neue `SettingsRow` "Insulinwirkdauer" mit `ICON.insulin` + `diaSub`

### 5. components/IOBCard.tsx
- `IOBSparkline` Props: `insulinType` ersetzt durch `diaMin: number`
- Haupt-Komponente: `userDiaMinutes = getInsulinSettings().diaMinutes` per `useMemo`; `diaMin = getDIAMinutes(insulinType, userDiaMinutes)`; `calcTotalIOB` mit `userDiaMinutes`

### 6. components/IOBHistoryChart.tsx
- `getInsulinSettings` importiert
- `diaMin` per `useMemo` aus `getInsulinSettings().diaMinutes` + `getDIAMinutes`

### 7. app/(protected)/entries/page.tsx
- `getInsulinSettings` importiert
- IOB-Sparkline-Berechnung verwendet `getDIAMinutes(insulinType, getInsulinSettings().diaMinutes)`

### 8. Übersetzungen (de + en)
- 6 neue Keys: `row_dia`, `subtitle_dia`, `sheet_dia_title`, `sheet_dia_body`, `sheet_dia_label`, `sheet_dia_hint`

### 9. Tests
- `tests/unit/iobCard.test.ts`: 5 neue Tests — Override gültig, Fallback bei < 60, Fallback bei NaN/undefined, `calcTotalIOB` mit kurzer DIA räumt früher ab
- `tests/unit/evaluation.test.ts` + `lifecycle.test.ts`: `diaMinutes: 180` zu allen `InsulinSettings`-Fixtures ergänzt (TS2741 gefixt)

### 10. DECISIONS.md
- Fix Log Eintrag für Task #555 ergänzt
- D-011 Eintrag: `dia_minutes`-Spalte nullable, Check 60–360, Begründung dokumentiert
