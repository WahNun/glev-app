# Fix Report — Task 433
## Telegram-Bot Phase 1 + Phase 2: Message-Bus, Notify-Script & Webhook-Receiver

**Datum:** 2026-05-20

## Was wurde gemacht

### Phase 2 (dieser Task) — Webhook-Receiver

**`app/api/telegram/webhook/route.ts`** (neu):
- POST-Handler empfängt Telegram-Updates
- Prüft `X-Telegram-Bot-Api-Secret-Token` Header gegen `TELEGRAM_WEBHOOK_SECRET` (→ 401 bei Mismatch)
- Text-Updates werden direkt gelesen
- Voice-Updates: Telegram-Datei herunterladen (`getFile` + File-Download), an OpenAI Whisper (`whisper-1`) senden, transkribierten Text verwenden
- `task_id` per Regex `/Task[:\s]+(\d+)/i` aus `message.reply_to_message.text` extrahieren
- `inbound`-Zeile in `agent_messages` schreiben (Supabase service role)
- Updates ohne Reply-Kontext oder ohne erkennbare task_id werden ignoriert (→ 200 OK)
- `setWebhook`-Curl-Befehl (inkl. `secret_token`) im File-Header als Kommentar dokumentiert

**D-009** in DECISIONS.md: Telegram als Agent-Kommunikationskanal (Message-Bus via Supabase)

### Phase 1 (vorheriger Task) — bereits erledigt
- `supabase/migrations/20260520_agent_messages.sql` — `agent_messages`-Tabelle mit RLS
- `scripts/notify-telegram.mjs` — ESM-Script mit Realtime-Subscription
- `telegram:notify` npm-Script in root `package.json`

## Checkliste
- [x] Supabase-Tabelle `agent_messages` existiert mit RLS (service role only)
- [x] `scripts/notify-telegram.mjs` schickt per Telegram und wartet via Supabase Realtime
- [x] `telegram:notify` npm-Script in package.json
- [x] `app/api/telegram/webhook/route.ts` — POST-Handler mit Secret-Check, Text+Voice, task_id-Extraktion, inbound-Insert
- [x] 401 bei Secret-Mismatch
- [x] setWebhook-Curl im File-Header dokumentiert

## Env-Vars (Vercel — nach Deploy setzen)
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_WEBHOOK_SECRET`
- `SUPABASE_URL` (bereits vorhanden)
- `SUPABASE_SERVICE_ROLE_KEY` (bereits vorhanden)
- `OPENAI_API_KEY` oder `AI_INTEGRATIONS_OPENAI_API_KEY` (bereits vorhanden)
