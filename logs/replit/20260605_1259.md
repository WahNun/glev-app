# Fix Report — CGM Setup-Anfrage E-Mail-Adresse ändern
**Task GID:** 1259
**Date:** 2026-06-05

## Was wurde gebaut

Minimale Konfigurationsänderung: Die Benachrichtigungs-E-Mail bei neuen CGM Setup-Anfragen geht jetzt an `cgm-setup@glev.app` statt `lucas@glev.app`.

## Änderungen

### `app/api/cgm/setup-request/route.ts`
- Zeile 132: `to: "lucas@glev.app"` → `to: "cgm-setup@glev.app"` in `resend.emails.send()`
- Zeile 114: Kommentar angepasst: „Send email notification to cgm-setup@glev.app"

## Kein DB-Migration nötig
Reine Konfigurationsänderung — kein Schema-Change, kein neuer Cloud-Service.

## DECISIONS.md
Fix-Log-Eintrag hinzugefügt. Kein neuer D-XXX-Eintrag nötig.
