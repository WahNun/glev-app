# Task Report: Show BZ check results directly on the meal node cluster on the glucose chart
**Asana GID:** 739
**Datum:** 2026-05-27

## Was wurde gebaut

Feature war bereits vollständig in Task #702 implementiert. Diese Task hat ausschließlich pre-existing failing Unit-Tests repariert, die den Code-Review blockierten.

### 1. bgCheckColor — 5-Zonen-Modell (MealNodeCluster.tsx)

- `bgCheckColor()` von 3-Zonen (hyper → amber) auf klinisch korrektes 5-Zonen-Modell umgestellt:
  - < 70 mg/dL → rot (Hypoglykämie)
  - 70–80 mg/dL → amber (Borderline low)
  - 81–160 mg/dL → grün (Zielbereich)
  - 161–180 mg/dL → amber (Borderline high)
  - > 180 mg/dL → rot (Hyperglykämie)
- Badge zeigt jetzt Einheit „mg/dL" (vorher nur Zahl)
- Badge-Rect von 36 px auf 56 px verbreitert

### 2. Unit-Tests bgCheckColor (mealNodeMath.test.ts)

- Alte 3-Zonen-Tests durch 11 neue Tests ersetzt, die das 5-Zonen-Modell vollständig abdecken (alle Grenzen inclusive/exclusive verifiziert)

### 3. Supabase-Dep-Injection (lib/meals.ts)

- `SaveMealDeps`-Interface um `_supabase?: typeof supabase` erweitert
- `saveMeal()` nutzt `const sb = _deps?._supabase ?? supabase` für alle DB-Operationen
- Root cause: `workers: 1` in Playwright-Config bedeutet geteilten Modul-Cache; Supabase-Env-Vars in Replit initialisieren einen echten Client, dessen Export nicht durch ein Fake überschrieben werden kann

### 4. fakeClient-Export (_fake-supabase-meals.ts)

- `fakeClient` von `const` zu `export const` geändert, damit Tests ihn als `_supabase`-Override übergeben können

### 5. mealBolusMirror.test.ts

- Alle 7 `saveMeal()`-Aufrufe übergeben jetzt `{ _supabase: fakeClient, _insertInsulinLog: spy.fn }` → kein echter Supabase-Call in der Test-Suite mehr

## Testergebnis

```
913 passed (20.3s)
```

Alle 913 Unit-Tests grün, 0 Failures.
