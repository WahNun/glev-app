# Fix Report — E2E-Tests für den Voice Intent Router
**Task GID:** 1042
**Date:** 2026-06-02

## Was wurde gebaut

Vollständige Test-Abdeckung für den Voice Intent Router (Task #1042):

- **22 Unit-Tests** für `classifyIntent()` in `tests/unit/intentClassifier.test.ts` — Bolus-Fast-Path-Regex (Einheiten/Units/ie/u-Suffixe, Dezimalkomma, Obergrenzen-Clamp, 0-Units-Ablehnung), Navigate-Fast-Path (Deutsch+Englisch, alle Screen-Namen), Slow-Path-Delegation an API.
- **10 Unit-Tests** für `handleClassifyPost` in `tests/unit/classifyIntentRoute.test.ts` — alle 7 `KNOWN_INTENT_TYPES` akzeptiert, unbekannter Typ → `fallback_chat`, leere/ungültige/whitespace-Antwort → `fallback_chat`, Netzwerkfehler → `fallback_chat` (kein 500).
- **4 E2E-Tests** in `tests/e2e/voice-intent-bolus.spec.ts` — dispatcht `glev:open-bolus-log`-Events und assertiert InsulinForm-Pre-fill: units=3 + name="NovoRapid", fehlender Name korrumpiert bestehenden Wert nicht, units=0 überschreibt nicht, bolus-Tab wird auch von basal aus erzwungen.

## Änderungen

### `app/api/ai/classify-intent/route.ts`
- `handleClassifyPost` und `KNOWN_INTENT_TYPES` exportiert (nötig für Route-Unit-Tests ohne Next.js-Bootup).

### `lib/ai/intentClassifier.ts`
- `NAVIGATE_RE`-Regex um fehlende Screen-Namen ergänzt (`insights`, `entries`, `engine` auf Englisch).

### `tests/unit/intentClassifier.test.ts` (neu)
- 22 Tests für Fast-Path + Slow-Path von `classifyIntent()`.

### `tests/unit/classifyIntentRoute.test.ts` (neu)
- 10 Tests für `handleClassifyPost` mit Mistral-Mock.

### `tests/e2e/voice-intent-bolus.spec.ts` (neu)
- 4 E2E-Tests mit Session-Reuse-Login (`ensureLoggedIn`) gegen echte Supabase-Test-User.

## Gelöstes Kernproblem: BzCheckModal-Dismiss in Headless-Playwright

`BzCheckModal` ist ein CSS-transform-Bottom-Sheet: immer im DOM, `translateY(100%)` wenn geschlossen — nie detached. Sein "Abbrechen"-Button liegt in headless Chromium unterhalb des Viewports.

Alle naiven Ansätze scheitern:
- `locator.click()` → "Element is outside of the viewport"
- `locator.click({ force: true })` → gleicher Fehler (force bypassed Viewport-Check nicht)
- `page.evaluate(() => btn.click())` → React-synthetische Events reagieren nicht auf JS-`.click()`

**Lösung:** `bzDialog.locator('input[type="number"]').focus()` + `page.keyboard.press("Escape")`. Der BzCheckModal hat `handleKeyDown` als React-`onKeyDown` direkt auf dem numerischen Input befestigt — Escape ruft `onClose()` auf. Playwright's `focus()` funktioniert auf Off-Screen-Elementen ohne Viewport-Einschränkung.

## TypeScript
Keine neuen Typ-Fehler eingeführt.

## Test-Ergebnisse
- Unit: 32/32 grün (24.9 s)
- E2E: 4/4 grün (34.7 s)
