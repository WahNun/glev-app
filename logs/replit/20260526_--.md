# Task Report: Dedizierte /settings/ai Sub-Page für alle KI-Einstellungen
**Asana GID:** —
**Datum:** 2026-05-26

## Was wurde gebaut

### 1. Neue Sub-Page `app/(protected)/settings/ai/page.tsx`
Vollständig selbstständige Seite nach dem Muster `/settings/icr-schedule`:
- ← Zurück-Link zu `/settings`
- Master-Toggle für Glev AI (Consent on/off)
- FAB-Mode-Umschalter (AI öffnen / Spracheingabe starten)
- TTS Auto-Read-Toggle
- Glev Intelligence Scopes (Mahlzeit, Glukose, IOB, Verlauf)
- Destruktiver „Alle AI-Zugriffe widerrufen"-Button
- Guard: `useFeatureFlag("ai_voice")` → redirect zu `/settings` wenn Flag off

### 2. `app/(protected)/settings/page.tsx` aufgeräumt
Entfernt (~220 Zeilen):
- AI-State-Variablen: `aiConsentGranted/Busy`, `aiScopeGlucose/Iob/History/Busy`, `fabMode`, `ttsAutoRead`
- Alle zugehörigen Handler: `toggleAiConsent`, `toggleAiScope`, `revokeAllAiAccess`
- Initial-Consent-Fetch + window-focus-Refresh useEffect
- Beide langen AI-SettingsSections (`section_glev_intelligence` + `section_glev_ai`)

Ersetzt durch: eine einzelne `SettingsRow` mit `ICON.sparkle` → `router.push("/settings/ai")`.

### 3. i18n (`messages/de.json` + `messages/en.json`)
4 neue Keys im `settings`-Namespace: `ai_page_title`, `ai_page_subtitle`, `ai_settings_row_subtitle`, `ai_settings_back`.

## Kein neuer D-XXX-Eintrag
Reine UI-Refactoring — kein Infrastruktur-/Schema-/Auth-/Compliance-Wechsel.
