# Task Report: Standard-Tier €9/Monat mit 7-Tage-Trial bauen
**Asana GID:** 1214277574885599
**Datum:** 2026-05-25

## Was wurde gebaut

### 1. DB-Migration
- `supabase/migrations/20260524_add_trial_end_at.sql` — fügt `trial_end_at TIMESTAMPTZ` (nullable) zu `profiles` hinzu. Bestehende User bleiben NULL → kein Trial.

### 2. API-Routen
- `app/api/auth/free-trial/route.ts` — POST, Bearer-Auth, Service-Role-Upsert `profiles.trial_end_at = NOW() + 7 Tage`, fire-and-forget E-Mail-Stub
- `app/api/email/trial-reminder/route.ts` — Stub, loggt nur; echter Drip-Call folgt im Folge-Task

### 3. Neue Seiten
- `app/signup/page.tsx` — öffentliche Free-Trial-Signup-Seite: E-Mail + Passwort → `supabase.auth.signUp()` → `POST /api/auth/free-trial` → `/onboarding`. Kein Stripe, keine Kreditkarte.
- `app/pro-trial/page.tsx` — vollständige Kopie der `/pro`-Seite mit zusätzlichem "7 Tage kostenlos testen"-Button (FreeTrialCTA) unter jedem Kauf-CTA

### 4. Angepasste Seiten
- `app/beta/page.tsx` — Hero + Pricing-Block um sekundären Button "7 Tage kostenlos testen" → `/signup` erweitert (bestehender Stripe-Flow bleibt erhalten)

### 5. Paywall-Modal
- `components/TrialExpiredModal.tsx` — Client-Komponente, prüft `profiles.trial_end_at < NOW()` + `GET /api/me/plan === "free"` → zeigt nicht-dismissbares Modal mit "Jetzt Pro werden →"-Link und Logout-Button
- `app/(protected)/layout.tsx` — `<TrialExpiredModal />` nach `<LowGlucoseAlarmTicker />` eingehängt

## Architektur-Entscheidungen
Alle dokumentiert in **D-023** (DECISIONS.md):
- Zwei Signup-Pfade: Payment-First (Stripe) vs. Free-Trial (direkt)
- Kein `trial` als eigener Plan-Typ — `trial_end_at + plan === "free"` reicht
- Paywall client-seitig (Modal), kein Middleware-Block

## Offene Folge-Tasks
- Echter Resend-Drip-Call in `/api/email/trial-reminder` (L-Trial Drip-Sequenz aus Sprint 8 Backlog)
- Stripe-Checkout aus dem Modal heraus verknüpfen (aktuell nur Link nach `/pro`)
