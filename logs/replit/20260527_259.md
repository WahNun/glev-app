# Task #259 — SPIKE_STRONG: Outcome-Coverage-Check + PDF-/CSV-Tests

## Summary
Show the new SPIKE_STRONG outcome consistently across all summary views.

## Changes

### 1. `scripts/check-outcome-coverage.mjs` (new)
Grep-based script that finds every file enumerating `SPIKE` in a switch or object literal and exits 1 if `SPIKE_STRONG` is missing from the same enumeration. Added `check:outcome-coverage` npm script to `package.json`. Confirmed 0 gaps remain after all fixes.

### 2. `components/AppMockupPhone.tsx`
Extended the local `EvalKind` type with `"SPIKE_STRONG"`. Added `evalColor["SPIKE_STRONG"] = "#FF6A00"` and `evalLabel["SPIKE_STRONG"]` = `"STARKER SPIKE"` (de) / `"STRONG SPIKE"` (en) so the marketing mockup renders correctly if a fixture uses this outcome.

### 3. `lib/pdfReport.tsx`
Added an 8th "Outcome" column to the Mahlzeiten table. Column widths adjusted to 15/9/23/8/8/12/12/13%. Cell text from `EVAL_LABELS[m.evaluation]`, cell color from `EVAL_COLORS[m.evaluation]`, bold for non-null evaluations.

### 4. `tests/unit/pdfReport.test.ts`
- Updated header-count test from 7 to 8 columns.
- Added 3 new tests: SPIKE_STRONG renders "Strong Spike" with `#FF6A00` color override in the style array, GOOD renders correctly, null evaluation renders "—" without crash.

### 5. `tests/unit/export.test.ts`
Added 2 new tests: `evaluation="SPIKE_STRONG"` round-trips verbatim through `mealsToCSV`, and SPIKE vs SPIKE_STRONG are distinguishable in the CSV output.

## Test Results
- 63 tests pass (including all 5 new tests)
- 3 pre-existing TIR/TBR/TAR failures unrelated to this task (tracked as follow-up #771)
- `node scripts/check-outcome-coverage.mjs` exits 0 — no coverage gaps
