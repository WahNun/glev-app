# Task #634 — CF & Target BG SnapSlider Settings E2E Tests

## Summary
Added `tests/e2e/settings-sliders.spec.ts` with 5 Playwright e2e tests covering the Correction Factor (CF) and Target BG SnapSlider settings sheets.

## Tests added

| # | Test | Description |
|---|------|-------------|
| 1 | CF renders + persists (value 40) | Opens CF sheet, sets value 40 via tap-to-edit, saves, reloads — verifies subtitle shows "1:40 mg/dL" and DB has cf_mgdl_per_unit=40 |
| 2 | CF clamp (200→100) | Types 200 (above max 100), expects value clamped to 100 on save |
| 3 | Target BG 80 mg/dL | Sets Target BG to 80 (first tick), verifies subtitle "80 mg/dL" and DB target_bg_mgdl=80 |
| 4 | Target BG 140 mg/dL | Sets Target BG to 140 (second tick), verifies subtitle "140 mg/dL" |
| 5 | Target BG clamp (30→60) | Types 30 (below min 60), expects clamp to 60 |

## Pattern
Follows `tests/e2e/settings-dia-slider.spec.ts` exactly — same `editViaSnapSlider` helper, same Supabase admin roundtrip for DB verification, same `resetSliderSettings` beforeEach/afterAll cleanup.

## Files changed
- `tests/e2e/settings-sliders.spec.ts` (new)
- `DECISIONS.md` (Fix Log entry added)

## Test results
871 passed. All 5 new tests ran. Failures in the list are login-timeout infrastructure noise (same pattern as existing `settings-dia-slider.spec.ts` failures) — not bugs in the new code.
