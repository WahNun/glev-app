# Fix Report — Apple Health als eigene Zeile in CGM-Einstellungen
**Task GID:** 1317
**Date:** 2026-06-07

## Was wurde gebaut

Apple Health wurde aus dem FreeStyle-Libre-Sheet in `CgmSettingsCard.tsx` herausgelöst und erscheint jetzt als eigenständige Top-Level-Zeile in den CGM-Einstellungen — gleichrangig neben Libre 2, Libre 3 und Nightscout.

## Änderungen

### `components/AppleHealthSettingsCard.tsx` (neu)
- Eigenständige Client-Komponente mit vollständigem Apple-Health-State (connect, status, permission-denied/revoked, backfill, steps, message)
- Handler: `handleAppleHealthConnect`, `handleAppleHealthDisconnect`, `handleBackfillWorkouts`, `handleBackfillSteps`
- UI: Beschreibungstext, Plattform-Hinweis (Web-only), Permission-Denied-Banner, Status-Card mit letztem Messwert + Freshness, Connect/Disconnect-Buttons, Backfill-Sektion (Workouts + Steps), Background-Delivery-Warnung

### `app/(protected)/settings/cgm/page.tsx`
- `"apple_health"` zu `SheetKey`-Union hinzugefügt
- `useAppleHealthConnected`-Hook eingebunden (Connected-Dot in der Zeile)
- Neue `SettingsRow` für Apple Health: Herz-Icon (`#FF2D55`), i18n-Key `row_apple_health`, Subtitle `subtitle_only_iphone` auf Web
- URL-Param-Handler korrigiert: `?open=apple_health` öffnet jetzt Sheet `"apple_health"` (vorher fiel es fälschlich auf `"libre2"`)
- `AppleHealthSettingsCard` in `sheetMap` eingetragen

### `components/CgmSettingsCard.tsx`
- Alle Apple-Health-State-Variablen entfernt (`appleHealthSelected`, `appleHealthStatus`, `appleHealthPermissionDenied`, `appleHealthPermissionRevoked`, `appleHealthSubmitting`, `appleHealthMessage`, `backfillRunning`, `backfillProgress`, `backfillResult`, `workoutsRange`, `stepsBackfillRunning`, `stepsBackfillProgress`, `stepsBackfillResult`)
- Handler entfernt (`handleAppleHealthConnect`, `handleAppleHealthDisconnect`, `handleBackfillWorkouts`, `handleBackfillSteps`, `loadAppleHealthState`)
- Apple-Health-Dropdown-Option entfernt
- `cgmType === "apple-health"`-JSX-Block (~570 Zeilen) entfernt
- Zugehörige Imports entfernt (`useGlucoseUnit`, `mgdlToMmol`, `formatLocalDate`, `formatRelativeAge`, `tAh`, `isNativePlatform`)
- Datei von ~1580 Zeilen auf ~1010 Zeilen reduziert

### `messages/de.json` + `messages/en.json`
- `settings.row_apple_health` — Zeilenbezeichnung "Apple Health" / "Apple Health"
- `settings.subtitle_only_iphone` — "Nur iPhone" / "iPhone only"

## Was nicht geändert wurde (bewusst)
- `lib/cgm/appleHealth.ts` — Backend-Logik unverändert
- API-Routes für Apple Health — unverändert
- `useAppleHealthConnected`-Hook — unverändert
