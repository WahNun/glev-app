# Fix Report — Task #236: Glev AI Button + AiHelperSheet

**Datum:** 2026-05-22  
**Task:** Glev AI Button (FAB-Umbau + Chat-Sheet)

## Was wurde gemacht

### 1. DB-Migration
- `supabase/migrations/20260522_ai_consent.sql` — neue Spalte `ai_consent boolean NOT NULL DEFAULT false` in `user_settings`

### 2. GlevAIButton-Komponente
- `components/GlevAIButton.tsx` — 64×64 runder FAB mit animiertem SVG-Icon, `onPress` + `isListening`-Props
- `components/GlevAIButton.module.css` — Breathing-Ring, Sweep-Shimmer, Orbital-Dots, Listening-State, Reduced-Motion

### 3. AiHelperSheet-Komponente
- `components/AiHelperSheet.tsx` — 85dvh Slide-up Sheet, Chat-UI (User/AI-Bubbles), 3 Quick-Chips, 800ms statische AI-Antwort, Engine-Deep-Link im Header, nicht-funktionale History/Settings/Mic-Icons

### 4. Layout.tsx — FAB-Integration
- Imports: `AiHelperSheet`, `GlevAIButton`, `fetchAiConsent`
- State: `aiSheetOpen`, `aiConsent`, `aiToast`; `useEffect` lädt `ai_consent` beim Mount
- `onShortPress`: Bei `voice.recording` → stop; bei `aiConsent` → Sheet öffnen; sonst → Toast 3s
- `MobileGlevFab` inner visual: `GlevLogo`-Bubble durch `<GlevAIButton pointerEvents="none">` ersetzt
- JSX: `<AiHelperSheet>` + Coming-soon-Toast hinzugefügt

### 5. lib/userSettings.ts
- `fetchAiConsent()` am Ende der Datei ergänzt (liest `user_settings.ai_consent` für eingeloggten User)

### 6. DECISIONS.md
- Fix Log Eintrag für Task #236 ergänzt
- D-010 Eintrag: `ai_consent`-Spalte in `user_settings` — Default false, Begründung dokumentiert

## Keine Breaking Changes
- Bestehende Nutzer: `ai_consent = false` (Default) → Toast statt Sheet
- Long-Press → `DashboardQuickAddSheet` unverändert
- Desktop-Sidebar unverändert
