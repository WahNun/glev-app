# Fix Report — TTS Voice Quality + calcBasalFraction Plateau + Ops-Page
**Task GID:** ad-hoc
**Date:** 2026-06-03

## Was wurde gebaut

Vier Fixes in der TTS-Pipeline, der IOB-Berechnung und der Ops-Seite.

## Änderungen

### `app/api/tts/mistral/route.ts`
- `useStylePrefix = !skipStylePrefix` — Style Prefix wird jetzt immer angewendet, auch wenn `ref_audio` gesetzt ist. Vorheriger Stand hatte den Prefix fälschlicherweise bei aktiver Voice-Clone deaktiviert, was robotische Aussprache verursachte.

### `app/glev-ops/mistral/page.tsx`
- Beschreibungstext korrigiert: "automatisch deaktiviert" → "weiterhin aktiv (verbessert Natürlichkeit auch beim Stimm-Klon)".
- Checkbox-Label: veralteten Klammer-Hinweis entfernt.
- `handleTest` zeigt jetzt `errJson.detail` aus dem 502-Body an — Mistral-Fehlermeldungen sind jetzt im Admin-Panel sichtbar.

### `hooks/useTTS.ts` — `extractAssistantText`
- `STRIP_BARE` → `allPrefixes`: fügt `"- <prefix>"`-Bullet-Varianten hinzu.
- Neue Keywords: `Stil:`, `Du bist Glev`, `Heute ist`, `Kontext-Snapshot`, Context-Preamble-Bullets.
- Alle 20 Unit-Tests bestehen.

### `lib/iob.ts` — `calcBasalFraction`
- Zweiphasiges Modell: Plateau bei 1.0 bis `windowMin × peakFraction`, dann linearer Tail auf 0.
- Standard `peakFraction = 0.6`.

### `tests/unit/calcBasalFraction.test.ts`
- Tests neu geschrieben für das zweiphasige Modell.
