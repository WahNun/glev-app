# Session Report — 2026-06-01 (ad-hoc)

## Erledigte Aufgaben

1. **Push-Test Button in Settings-Seite** (`app/(protected)/settings/app/page.tsx`)
   - `PushDebugSection` erweitert: Buttons-Reihe mit "Push-Registrierung neu starten" + Sandbox-Checkbox + "🔔 Test-Push"-Button
   - Button disabled solange kein Token in localStorage; Ergebnis grün/rot darunter

2. **Self-Test Endpoint** (`app/api/push/self-test/route.ts`)
   - Session-authentifiziert (Supabase Cookie, kein Admin-Secret nötig)
   - Liest `push_token + push_platform` aus `profiles` für den eingeloggten User
   - Sendet APNs (http2 + ES256-JWT) oder FCM Test-Push

3. **Admin Push-Test Route Fixes** (`app/api/admin/push-test/route.ts`)
   - Auth-Header: `x-admin-secret` → `Authorization: Bearer` (konsistent mit anderen Admin-Routen)
   - Import-Pfad: `@/lib/supabase/admin` → `@/lib/supabaseAdmin`

## Offene Punkte
- APNS_KEY_P8, APNS_KEY_ID, APNS_TEAM_ID, APNS_BUNDLE_ID müssen in Vercel Env-Vars eingetragen werden
- Nach Vercel-Redeploy kann der Button in Settings → App → Push-Debug-Panel genutzt werden
