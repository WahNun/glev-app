# DECISIONS.md

Dieses Dokument hält bewusste Architektur- und Scope-Entscheidungen fest sowie einen chronologischen Log aller abgeschlossenen Tasks. Zukünftige Agents lesen es am Task-Start, um den Kontext zu verstehen.

---

## Decisions

> Was hier steht, ist eine bewusste Nicht-Entscheidung oder eine Weichenstellung, die nicht mehr offen diskutiert werden soll.

### D-001 · Supabase statt eigener Auth/DB-Infrastruktur (2026-01-15)
Supabase liefert PostgreSQL, Row Level Security und Auth (Email/Password + JWT) als verwalteten Service. Eine eigene Postgres-Instanz mit Auth-Stack hätte dieselbe Betriebslast ohne den Mehrwert. Firebase wurde geprüft, aber wegen SQL-Querybarkeit und Open-Source-Kompatibilität verworfen. **Nicht wieder öffnen:** Solange keine Multi-Tenant-SaaS-Isolation erforderlich ist, bleibt Supabase die Datenebene.

### D-002 · Capacitor statt React Native für mobile Shells (2026-01-20)
Glev ist primär eine Next.js-Web-App. Capacitor erlaubt es, exakt dieselbe Web-Codebasis als Thin-Webview-Shell auf iOS/Android zu verpacken — kein zweiter Rendering-Pfad, keine doppelte Feature-Implementierung. React Native hätte eine separate Komponentenbibliothek und Logik-Schicht erfordert. **Nicht wieder öffnen:** Solange die App keine tief nativen UI-Patterns (z. B. UIKit-Navigation) braucht, bleibt Capacitor die Native-Schicht.

### D-003 · Keine direkten Dosis-Anweisungen in der UI (2026-02-01)
Glev ist ein Entscheidungs-*Support*-System, kein Medizinprodukt Klasse IIb. Direkte Dosis-Anweisungen (z. B. „Nimm jetzt 4 IE") würden eine klinische Validierung und MDR-Zertifizierung erfordern, die den aktuellen Rahmen sprengen. Alle Engine-Empfehlungen sind als Gesprächsbasis fürs Diabetes-Team gerahmt. **Nicht wieder öffnen:** Selbst nach einer möglichen MDR-Einreichung braucht jede Änderung an diesem Prinzip eine explizite Freigabe durch das medizinische Verantwortungsteam.

### D-004 · next-intl statt i18next für Lokalisierung (2026-02-10)
`next-intl` ist für Next.js App Router (Server Components, `generateMetadata`, Middleware-basiertes Locale-Routing) nativ ausgelegt. i18next benötigt zusätzliche Adapter (`next-i18next`) und arbeitet primär Client-seitig, was SSR-Hydration-Mismatches erzeugt. Unterstützte Locales: `de` (Standard) und `en`. **Nicht wieder öffnen:** Eine Migration würde alle `useTranslations()`-Aufrufe und die Middleware betreffen — Aufwand ohne funktionalen Mehrwert.

### D-005 · Pump-Dosierung bewusst aus dem Engine-Scope ausgeschlossen (2026-02-15)
Die Glev Engine berechnet Bolusempfehlungen für ICT-Nutzer (Pen-Injektionen). Insulinpumpen-Nutzer haben eigene Basal-/Bolusprofile, die von der Engine nicht modelliert werden. Die Engine zeigt Pump-Trägern kein Ergebnis an (oder einen expliziten Hinweis). Pump-spezifische Kalkulation (TBR, Extended Bolus) ist kein aktueller Roadmap-Punkt. **Nicht wieder öffnen:** Pump-Support erfordert ein eigenes Datenmodell und klinische Expertise — separates Feature-Flag, wenn überhaupt.

### D-006 · Vercel als einzige Production-Plattform, Replit nur Dev (2026-03-01)
Replit-Secrets fließen nicht in den Vercel-Build. Alle Produktions-Envvars leben in Vercel Project Settings. GitHub Actions (`flush-outbox.yml`) und Stripe-Webhooks zeigen direkt auf `https://glev.app`. Replit wird ausschließlich für Entwicklung und Agent-Tasks genutzt. **Nicht wieder öffnen:** Eine Vermischung der beiden Umgebungen (z. B. Replit Deploy) würde Webhook-Endpunkte und Cron-Jobs duplizieren und Secret-Management verkomplizieren.

### D-007 · Engine-Algorithmus: gewichteter k-NN-Ansatz statt LLM-only (2026-05-20)

**Warum kein LLM-only-Approach?**
Ein LLM könnte prinzipiell aus dem Freitext-Meal-Log direkt eine Dosisempfehlung generieren. Das wurde bewusst verworfen:
- **Nicht deterministisch:** Gleiche Eingabe, unterschiedliche Ausgabe — bei klinischen Empfehlungen nicht akzeptabel.
- **Nicht personalisierbar:** Ein generisches LLM kennt nicht das individuelle ICR, die Insulinsensitivität oder das Tageszeit-Muster des Nutzers.
- **Nicht auditierbar:** Der Reasoning-Pfad eines LLM ist für Compliance-Zwecke (MDR) nicht hinreichend dokumentierbar.
- **Latenz & Kosten:** Jede Dosisempfehlung würde einen API-Call erfordern; beim gewichteten Ansatz ist das eine lokale Pure-Funktion.

**Gewählter Ansatz: personalisierter, gewichteter Durchschnitt (k-NN-ähnlich)**
Die Engine lernt den ICR des Nutzers aus finalisierten Mahlzeiten (`carbs / insulin`) und gewichtet schlechtere Outcomes (OVERDOSE, UNDERDOSE → 0.3; SPIKE → 0.7) geringer als GOOD (1.0). Das ist konzeptionell verwandt mit einem gewichteten k-Nearest-Neighbour, aber ohne explizite Feature-Distanz — der Nutzungskontext (Tageszeit, Meal-Type) wird über separate Buckets abgedeckt, nicht über eine Ähnlichkeitsmetrik.

**Was das LLM tut (und was nicht):**
- GPT (`lib/ai/systemPrompt.ts`) parst den Freitext-Meal-Log und klassifiziert die Mahlzeit in einen der vier Typen (FAST_CARBS / HIGH_FAT / HIGH_PROTEIN / BALANCED).
- GPT schlägt **niemals** eine Dosis vor und wertet **niemals** eine Dosis aus — das ist im System-Prompt explizit verboten.
- Die Klassifikation fließt in die Spike-Cutoffs (FAST_CARBS=70, HIGH_FAT=40, HIGH_PROTEIN=50, BALANCED=55 mg/dL) und die Meal-Type-Farb-/Label-Darstellung.

**Vollständige Algorithmus-Dokumentation:** `docs/engine-algorithm.md`

**Nicht wieder öffnen:** Solange Glev kein Klasse-IIb-Medizinprodukt ist und keine klinische Validierung eines LLM-Dosierungsmodells vorliegt, bleibt die deterministische, auditierbare Pure-Function-Pipeline die einzige Dosis-Logik.

### D-009 · Telegram als Agent-Kommunikationskanal (Message-Bus via Supabase) (2026-05-20)

Der Agent (Replit) kann Lucas Fragen stellen und auf Antworten warten, ohne dass Lucas Replit öffnen muss. Der Kanal besteht aus drei Teilen:

1. **Supabase-Tabelle `agent_messages`** — `outbound`-Zeilen werden vom Script geschrieben, `inbound`-Zeilen vom Webhook-Receiver. RLS: service role only (kein anon/authenticated-Zugriff).
2. **`scripts/notify-telegram.mjs`** — ESM-Script, schreibt `outbound`-Zeile, schickt Telegram-Nachricht, wartet via Supabase-Realtime-Subscription auf `inbound`-Antwort (max. 10 Minuten, dann `TIMEOUT`).
3. **`app/api/telegram/webhook/route.ts`** — Next.js-Route, prüft `X-Telegram-Bot-Api-Secret-Token`, transkribiert Voice-Nachrichten via OpenAI Whisper (`whisper-1`), extrahiert `task_id` per Regex aus dem Reply-Kontext, schreibt `inbound`-Zeile.

**Warum Telegram statt Email/SMS?** Niedrige Latenz (Sekunden statt Minuten), native Reply-Thread-Unterstützung (kein neues Compose-Fenster), kein SMS-Provider-Setup. **Warum Supabase als Bus statt direktem Polling der Telegram-API?** Das Script hat keinen öffentlich erreichbaren Port und kann nicht als Webhook-Receiver fungieren. Supabase-Realtime ist der einzige zuverlässige Kanal zwischen dem Webhook (Vercel) und dem wartenden Script (Replit).

**Nicht wieder öffnen:** Solange der Agent auf Replit und die Produktion auf Vercel läuft, ist dieser hybride Ansatz (Webhook auf Vercel empfängt, Supabase-Bus transportiert, Script auf Replit wartet) die richtige Architektur. Eine direkte Polling-Lösung würde den Telegram-Timeout überschreiten und eine Long-Poll-Infrastruktur erfordern.

### D-008 · Engine-Schwellenwerte als exportierte Konstanten mit Doc-Sync-Check (2026-05-20)

Alle klinisch relevanten numerischen Schwellenwerte der Engine-Pipeline werden als `export const` in ihren jeweiligen Quelldateien gepflegt und sind nicht mehr als Inline-Literale vergraben:

- `lib/engine/evaluation.ts`: `HYPO_THRESHOLD`, `SPIKE_CUTOFF_FAST_CARBS`, `SPIKE_CUTOFF_HIGH_FAT`, `SPIKE_CUTOFF_HIGH_PROTEIN`, `SPIKE_CUTOFF_BALANCED` (neu); `SPEED_SPIKE_MGDL_PER_MIN`, `SPEED_SPIKE_STRONG_MGDL_PER_MIN`, `SPIKE_STRONG_MAGNITUDE_MULTIPLIER` waren bereits exportiert.
- `lib/engine/recommendation.ts`: `DEFAULT_ICR`, `DEFAULT_CF`, `DEFAULT_TARGET`, `SAFETY_BG_MIN`, `MAX_DOSE_UNITS` (neu exportiert).
- `lib/engine/adaptiveICR.ts`: `MIN_BUCKET_SAMPLES`, `OUTCOME_WEIGHT` (neu exportiert).

`docs/engine-algorithm.md` enthält jetzt einen maschinenlesbaren **Threshold Index** (letzter Abschnitt) mit Konstantenname, Quelldatei und Wert. `scripts/check-engine-doc-thresholds.mjs` liest diese Tabelle und vergleicht jeden Eintrag gegen die tatsächlichen Werte im TypeScript-Source. Aufruf: `pnpm run check:engine-doc` (Exit 1 bei Abweichung).

**Warum dieser Ansatz statt reiner Inline-Kommentare?** Inline-Kommentare wie "see X in Y" erinnern nur — sie verhindern nicht, dass der Wert still falsch wird. Die maschinenlesbare Tabelle + Script schließt diese Lücke ohne einen vollständigen TypeScript-Build zu erfordern (das Script ist reines Node.js ESM mit Regex-Extraktion).

**Nicht wieder öffnen:** Wenn neue klinische Schwellenwerte zur Engine hinzugefügt werden, müssen sie (a) als `export const` definiert und (b) im Threshold Index eingetragen werden. `pnpm run check:engine-doc` sollte in CI-Pipelines und vor jedem Release-Build laufen.

---

## Fix Log

| Datum | Task-Name | Asana-GID | Beschreibung |
|-------|-----------|-----------|--------------|
| 2026-05-22 | Dashboard Insulin-Karten: Flip-Rückseiten und Disclaimer vervollständigen | 519 | `app/(protected)/dashboard/page.tsx` — `NonMealLightExpand`: Disclaimer-Footer (`dashboard.iob_bg_hint`) am unteren Rand des aufgeklappten Bereichs ergänzt (eigene Zeile, `borderTop: 1px solid var(--border)`, `paddingTop: 8`, `fontSize: 10`, `color: var(--text-faint)`, immer sichtbar, nicht bedingt). `RateTripletCard`-Rückseite: Disclaimer-Div um fehlende `marginTop: "auto"`, `borderTop: "1px solid var(--border)"` und `paddingTop: 8` ergänzt — jetzt identisch zur `ControlScoreCard`- und IOBCard-Rückseite. Keine neuen i18n-Keys nötig (Key `dashboard.iob_bg_hint` existiert in de+en). Keine neuen D-XXX-Einträge (kein Infrastruktur-/Schema-/Auth-/Compliance-Wechsel). |
| 2026-05-22 | Flip-Rückseite für IOB-History-, Control-Score- und Rate-Triplet-Karte | 517 | `components/IOBHistoryChart.tsx`: Flip-Wrapper (perspective, rotateY, preserve-3d) ergänzt; 12h/24h-Toggle-Buttons und Peak-Popover-Klicks per `stopPropagation` geschützt; Rückseite zeigt Erklärung + DIA-Berechnungsprinzip + Disclaimer. `app/(protected)/dashboard/page.tsx` — `RateTripletCard`: useState+flip ergänzt, Rückseite erklärt Gut/Spike/Hypo-Raten + Berechnungsbasis. `ControlScoreCard`: `showDetails`/`showHow`-Accordion-Buttons entfernt, durch `flipped` ersetzt; Rückseite zeigt Aufschlüsselung (Balkendiagramm), Formel und Disclaimer; Vorderseite: nur Header, Score-Zahl, Fortschrittsbalken. Alle Rückseiten zeigen `iob_bg_hint`-Disclaimer immer sichtbar. 11 neue i18n-Keys in `messages/de.json` + `messages/en.json`: `control_score_back_body`, `iob_history_back_title/body/how`, `rate_triplet_back_title/body/how`. Kein neuer D-XXX-Eintrag nötig (kein Infrastruktur-/Schema-/Auth-/Compliance-Wechsel). |
| 2026-05-22 | Entries-Listenansicht durch Einzelkarte mit Pfeil-Navigation ersetzen | 516 | `app/(protected)/entries/page.tsx`: `currentIndex`-State eingeführt (initial 0). `filtered` von inline `rows.filter(...)` auf `useMemo([rows, filters, search, dateBounds])` umgestellt. Neuer `useEffect([filters, search, rows])` setzt Index + expanded + editingId auf 0/null zurück bei Filter-/Such-Wechsel. Deep-Link-`useEffect` nach `filtered`-Deklaration verschoben und aktualisiert: setzt `currentIndex` auf den gefundenen Index statt `scrollIntoView`. Listenrender (`filtered.map(r => {...})`) durch IIFE-basierter Einzelkarten-Darstellung ersetzt: ‹-Button (disabled bei Index 0) + `filtered[currentIndex]` + ›-Button (disabled am letzten Eintrag) + Counter „X / Y". Kein Redesign der Karten-Inhalte — collapsed und expanded Views bleiben identisch. |
| 2026-05-22 | Engine Dose Disclaimer (HWG-Compliance) | — | `engine/page.tsx`: Disclaimer-Zeile direkt unter dem Richtwert-Wert eingefügt (`fontSize: 11, color: var(--text-faint)`). Neuer i18n-Key `engine.dose_disclaimer` in `messages/de.json` („Orientierungswert — kein Ersatz für ärztliche Beratung.") und `messages/en.json` („Contextual estimate — not a substitute for medical advice."). `marginBottom` des Dose-Flex-Row von 12 → 4 px angepasst (Disclaimer übernimmt den Abstand). |
| 2026-05-22 | Badge-Text Fix (HWG-Compliance): iob_active + iob_covers_dose | — | `messages/de.json` + `messages/en.json`: `iob_active` von „⚡ Noch aktiv … Empfehlung bereits bereinigt" → „ℹ️ Noch aktiv … Richtwert angepasst"; `iob_covers_dose` von „✓ Kein zusätzliches Insulin nötig …" → „ℹ️ IOB deckt die Schätzung ab". Beide Keys waren bereits per Task #511 auf i18n migriert — nur Text geändert, kein Code-Umbau. EN-Keys analog angepasst. HWG-Compliance: keine direkten Dosis-Anweisungen, keine Aussage „kein Insulin nötig". |
| 2026-05-22 | Add an automated test that catches the IOB popover deep-link before it breaks | 507 | `tests/unit/iobCard.test.ts` um Section 10 „Deep-link IDs: insulinLogId + mealId" erweitert — 7 neue Playwright-Unit-Tests: bolus-log mit `id` setzt `insulinLogId`; bolus-log ohne `id` lässt `insulinLogId` `undefined` (Backward-Compat); Mahlzeit-Dose setzt `mealId` auf die Mahlzeit-UUID; Mahlzeit-Dose hat kein `insulinLogId` (Source-Trennung); bolus-Dose hat kein `mealId` (Source-Trennung); mehrere bolus-Logs tragen je ihre eigene `insulinLogId`; Mix aus bolus-Log und unverknüpfter Mahlzeit — IDs unabhängig korrekt. Alle 48 Tests grün in 3,9 s (PLAYWRIGHT_SKIP_WEBSERVER=1). Kein neuer D-XXX-Eintrag nötig (kein Infrastruktur-/Schema-/Auth-/Compliance-Wechsel). |
| 2026-05-22 | Guard the getActiveDosesAtTime helper with unit tests so chart popovers never show wrong doses | 503 | `tests/unit/iobActiveDoses.test.ts` angelegt mit 7 Unit-Tests für `getActiveDosesAtTime` (lib/iob.ts): leeres Doses-Array → leeres Ergebnis; Dose exakt bei tMs (elapsed=0) eingeschlossen; Dose bei tMs−diaMin (elapsed=diaMin) ausgeschlossen (strict less-than); Future-Dose (elapsed<0) ausgeschlossen; Mix aus 5 Dosen → nur die 2 aktiven zurückgegeben; 1 Minute vor Clearance noch eingeschlossen; 1 Minute nach Administration eingeschlossen. Alle 7 Tests grün in 3,6 s (PLAYWRIGHT_SKIP_WEBSERVER=1). |
| 2026-05-22 | Make insulin log rows in the IOB popover tappable too | 501 | `lib/iob.ts`: `id?: string` zu `InsulinLike` hinzugefügt; `insulinLogId?: string` zu `BolusDose` hinzugefügt; `buildDoses()` befüllt `insulinLogId: l.id` für bolus-basierte Insulin-Einträge. `components/IOBHistoryChart.tsx`: `isInsulin = d.source === "insulin" && !!d.insulinLogId` und `isTappable = isMeal || isInsulin` eingeführt; `handleClick` navigiert für Insulin-Zeilen zu `/entries#insulin-<id>`; Chevron, Cursor und Textfarbe auf `isTappable` umgestellt (beide Typen erhalten gleiche visuelle Behandlung). `app/(protected)/entries/page.tsx`: `NonMealRow` erhält optionalen `id?: string`-Prop, der auf dem äußeren `<div>` landet. `BolusRowCard` gibt `id={`entry-insulin-${log.id}`}` weiter. Deep-Link-Handler erweitert: erkennt `#insulin-<uuid>`-Prefix, mappt auf die richtige Insulin-Log-ID, ruft `setExpanded(insulinId)` + `scrollIntoView` auf `entry-insulin-<id>` auf. Alle 61 bestehenden IOB-Unit-Tests weiterhin grün. Kein neuer D-XXX-Eintrag (kein Infrastruktur-/Schema-/Auth-/Compliance-Wechsel). |
| 2026-05-22 | Add a test to make sure tapping a dose row actually opens the right meal | 500 | `tests/unit/iobPeaks.test.ts` um 5 neue Unit-Tests für `buildDoses()` erweitert: verifiziert `mealId === meal.id` für mahlzeit-basierte Dosen, `mealId === undefined` für Insulin-Log-Dosen, Koexistenz beider Quellen, kein mealId-Leak bei verlinkten Mahlzeiten, und unabhängige mealId-Befüllung bei mehreren Mahlzeiten. 24 Tests insgesamt, alle grün in 4,4 s. `tests/e2e/iob-dose-row-navigation.spec.ts` neu erstellt: Playwright-E2E-Test, der eine synthetische Mahlzeit mit `insulin_units: 8` via Admin-Client seed't, im Dashboard auf den IOB-Peak-Pill (`svg g[role="button"]`) klickt, die Mahlzeit-Dosis-Zeile (`role="link"`) antippt und prüft, dass (a) URL zu `/entries#<mealId>` navigiert und (b) `#entry-<mealId>` sichtbar und mit "IOB nav test meal"-Text expanded ist. Locator-Design-Entscheidung: CSS `svg g[role="button"]` statt aria-label-Regex, da der Peak-Label einen locale-sensitiven Zeit-String enthält (en-US: "1:30 AM", de-DE: "13:30"). Test läuft in ~10 s. Kein neuer D-XXX-Eintrag nötig. |
| 2026-05-22 | Let users tap a dose row in the IOB peak popover to jump to that meal entry | 499 | `lib/iob.ts`: `mealId?: string` zu `BolusDose` hinzugefügt; `buildDoses()` befüllt `mealId: m.id` für mahlzeit-basierte Dosen. `components/IOBHistoryChart.tsx`: `useRouter` importiert, Dose-Rows im Peak-Popover für Mahlzeiten tappbar gemacht — `onClick` ruft `router.push("/entries#${d.mealId}")` auf, sodass die Einträge-Seite über den bestehenden Hash-Deep-Link die Zeile aufklappt und scrollt. Mahlzeit-Zeilen erhalten einen subtilen Chevron (inline SVG 7×10 px), hellere Textfarbe (`--text` statt `--text-dim`) und `cursor: pointer`. Manuelle Bolus-Zeilen bleiben no-op (kein Chevron, kein Cursor). Kein neuer D-XXX-Eintrag nötig (kein Infrastruktur-/Schema-/Auth-Wechsel). |
| 2026-05-22 | Catch IOB history regressions automatically before they reach users | 494 | `tests/unit/iobHistory.test.ts` erweitert mit 5 neuen Tests (15 total) für `buildIOBHistory` (lib/iob.ts): Dose exakt an DIA-Grenze → iob=0 am letzten Sample (rapid 180 min + regular 300 min); nowMs-Anker verifiziert — erstes Sample = nowMs−hours×60×60000, letztes Sample = nowMs (je mit intervalMin=15 und intervalMin=5 geprüft). Alle 15 Tests grün in 4,6 s (PLAYWRIGHT_SKIP_WEBSERVER=1). Bereits vorhandene Tests deckten ab: pre-Dose-Samples = 0, future-Dose = 0, IOB-Peak nahe Administrationszeitpunkt, DIA-Clearance (>DIA), zwei gestapelte Dosen akkumulieren, Rapid vor Regular gecleant, Sampling-Count = steps+1, monotoner Decay nach Peak. |
| 2026-05-22 | Let users write automated tests that verify IOB peak detection edge cases | 491 | `detectIOBPeaks(samples: IOBSample[]): IOBPeak[]` als exportierte Pure Function in `lib/iob.ts` ergänzt (vorher inliniertes `useMemo` in `IOBHistoryChart.tsx`). `IOBHistoryChart.tsx` ruft jetzt `detectIOBPeaks` auf und ergänzt SVG-Koordinaten für die Marker. `tests/unit/iobPeaks.test.ts` mit 19 Playwright-Unit-Tests angelegt: leeres/ein-/zwei-Element-Array, Index-0-und-letzter-Index-kein-Peak, 0.5-IE-Schwelle (exakt / darunter / darüber), flaches Plateau, einzelner klarer Peak, zwei Peaks sortiert, Top-3-Cap bei 5 Peaks, einzelner Dose-Day, perfekt flaches Array, Plateau-Kante, minimales 3-Element-Array. Alle 19 Tests grün in 14 s. |
| 2026-05-20 | DECISIONS.md anlegen und in Abschluss-Flow einbinden | 1209934567890123 | Initiale Anlage von DECISIONS.md, Erweiterung von finalize-task.sh um Pflicht-Check, Prozessregeln in replit.md eingetragen. |
| 2026-05-20 | Backfill key architecture decisions into DECISIONS.md | 416 | 6 Entscheidungen (D-001–D-006) in ## Decisions eingetragen: Supabase, Capacitor, Kein-Dosis-Imperativ, next-intl, Pump-Ausschluss, Vercel-als-Prod. |
| 2026-05-20 | Keep the architecture decision log up to date automatically | 417 | finalize-task.sh um Architektur-Grenz-Check erweitert (supabase/, capacitor.config, middleware.ts, lib/emails/, next.config, .github/workflows/, pnpm-workspace.yaml, package.json): bei Treffer Reminder ohne Exit-Code-Fehler. Self-Assessment-Checkliste in replit.md § Agent Workflow Rules (Regel 3) eingetragen. |
| 2026-05-20 | Document the Glev Engine recommendation algorithm so future changes don't break its logic | 418 | `docs/engine-algorithm.md` angelegt mit vollständiger Beschreibung aller Engine-Schichten (Klassifikation, Lifecycle, Evaluation, Adaptive ICR, Dose Recommendation, Pattern Detection) inkl. aller Schwellenwerte, Invarianten und Safety-Gates. D-007 in DECISIONS.md: begründet warum gewichteter k-NN-Ansatz statt LLM-only. |
| 2026-05-20 | Telegram-Bot Phase 1: Message-Bus + Notify-Script | 1214985739346044 | Supabase-Migration `20260520_agent_messages.sql` (agent_messages-Tabelle, RLS service-role-only), `scripts/notify-telegram.mjs` (ESM, Realtime-Subscription, 10-min-Timeout), npm-Script `telegram:notify` in package.json ergänzt. |
| 2026-05-20 | Keep the Engine algorithm doc automatically in sync when thresholds change | 422 | Alle numerischen Schwellenwerte aus evaluation.ts, recommendation.ts und adaptiveICR.ts als exportierte Konstanten herausgezogen (HYPO_THRESHOLD, SPIKE_CUTOFF_*, SAFETY_BG_MIN, MAX_DOSE_UNITS, DEFAULT_ICR/CF/TARGET, MIN_BUCKET_SAMPLES). docs/engine-algorithm.md mit Konstantennamen annotiert und einen maschinenlesbaren "## Threshold Index" ergänzt. scripts/check-engine-doc-thresholds.mjs prüft, ob die Werte in der Tabelle mit den exportierten Konstanten übereinstimmen; läuft via `pnpm run check:engine-doc`. D-008 in ## Decisions. |
| 2026-05-20 | Add automated tests that guard every Engine threshold so a wrong number is caught immediately | 421 | `tests/unit/engineThresholds.test.ts` angelegt mit 25 Boundary-Tests: Spike-Cutoffs (FAST_CARBS=70, HIGH_FAT=40, HIGH_PROTEIN=50, BALANCED=55) je cutoff vs cutoff+1; SPIKE_STRONG Magnitude 1.5×cutoff (alle 4 Klassen) und Speed-Grenze 2.49→SPIKE / 2.50→SPIKE_STRONG; BG-Floor BG=79→blocked / BG=80→nicht geblockt; Dose-Ceiling 26u→25u und 25u unverändert; Confidence-Bands sampleSize 4/5/9/10; Pattern-Raten overdose/underdose/spike an der 0.50/0.40-Grenze. Fixture-Erkenntnis: bgBefore muss ≥110 sein wenn delta<-30, sonst löst HYPO_DURING aus (bgAfter<70). |
| 2026-05-20 | Run the Engine doc threshold check automatically before every release build | 425 | `"prebuild": "node scripts/check-engine-doc-thresholds.mjs"` in package.json ergänzt — läuft automatisch vor jedem `next build` (also auch im Vercel-Build). `.github/workflows/engine-doc-check.yml` hinzugefügt: Job läuft auf push/PR gegen main, checkt aus, setzt Node 20, führt das Script aus — Exit 1 bei Abweichung blockiert den Merge/Deploy. Kein neuer D-XXX-Eintrag nötig (D-008 deckt das bereits ab, expliziter Hinweis war dort schon: "sollte in CI-Pipelines und vor jedem Release-Build laufen"). |
| 2026-05-20 | Catch a changed Engine threshold automatically with a unit test | 426 | Boundary-Tests für alle vier Spike-Cutoffs (FAST_CARBS=70, HIGH_FAT=40, HIGH_PROTEIN=50, BALANCED=55), HYPO_THRESHOLD=70 und SAFETY_BG_MIN=80 in tests/unit/evaluation.test.ts und tests/unit/recommendation.test.ts ergänzt. Tests importieren die Konstanten direkt — Wert- oder Namensänderung bricht den Test sofort. Strict-`>`/`<`-Grenze korrekt abgebildet: delta=cutoff → UNDERDOSE, delta=cutoff+1 → SPIKE; bgAfter=threshold → kein Hypo, bgAfter=threshold-1 → HYPO_DURING; currentBG=floor → nicht geblockt, currentBG=floor-1 → geblockt. |
| 2026-05-20 | Telegram-Bot Phase 1 + Phase 2: Message-Bus, Notify-Script & Webhook-Receiver | 433 | Phase 2 abgeschlossen: `app/api/telegram/webhook/route.ts` erstellt — POST-Handler prüft `X-Telegram-Bot-Api-Secret-Token` (401 bei Mismatch), verarbeitet Text- und Voice-Updates (Voice via OpenAI Whisper `whisper-1` transkribiert), extrahiert `task_id` per Regex `/Task[:\s]+(\d+)/i` aus Reply-Kontext, schreibt `inbound`-Zeile in `agent_messages`. setWebhook-Curl-Befehl als Kommentar im File-Header dokumentiert. D-009 neu eingetragen. Phase 1 (Migration + Script) war bereits in vorherigem Task erledigt. |
| 2026-05-21 | IOB-Visualisierung: Dashboard-Gauge + Entries-Sparkline + i18n | 1215035278730472 | `components/IOBCard.tsx` neu erstellt: SVG-Kreis-Gauge (r=44, strokeWidth=9), Farbcodierung grün/amber/orange nach <1/1–3/>3 IE, Auto-Refresh alle 5 min + window-focus. Dashboard: `insulinType`-State + `fetchInsulinType`-Fetch; IOBCard als dritte Karte im Glukose-Cluster. Entries-expanded-view: parabole Wirkprofil-SVG nach dem CLASSIFICATION-Block (grau=vergangen, farbig=verbleibend, gestrichelte Zeitmarke). 6 neue i18n-Keys im `dashboard`-Namespace (de+en), 3 neue Keys im `entriesExpand`-Namespace. |
| 2026-05-20 | Make sure the Telegram webhook can't be flooded or abused | 436 | `app/api/telegram/webhook/route.ts` um drei Schutzschichten erweitert: (1) IP-Rate-Limiting — Sliding-Window 10 req/min per Source-IP via module-level Map, 429 bei Überschreitung; (2) update_id-Deduplizierung — module-level bounded Set (max 500 Einträge, FIFO-Eviction), doppelte update_ids werden mit 200 ok quittiert ohne weitere Verarbeitung; (3) Voice-Duration-Cap — Sprachnachrichten länger als 60 s werden mit 413 abgelehnt bevor Telegram-Download oder Whisper-Call ausgeführt wird. Kein neuer D-XXX-Eintrag nötig (kein Infrastruktur- oder Schema-Wechsel, nur Härtung einer bestehenden Route). |
| 2026-05-20 | Let the agent ask Lucas a question automatically when finishing a task | 435 | `scripts/ask-telegram.mjs` erstellt: schlanker Wrapper um notify-telegram.mjs mit numerierten Optionen, klarer Formatierung (❓-Header) und graceful degradation (gibt SKIPPED aus wenn Secrets fehlen, TIMEOUT nach 10 Min). `finalize-task.sh` um optionalen `--ask "Frage?" [opt1] [opt2]`-Flag erweitert: fragt Lucas per Telegram vor dem Commit, druckt Antwort, fährt dann fort. `package.json`: npm-Script `telegram:ask` ergänzt. `replit.md` § Agent Workflow Rules: neue Regel 4 beschreibt wann und wie der Agent Lucas fragen soll. |
| 2026-05-21 | Telegram-Webhook: Diagnose & Fix (Antworten kommen nicht im Shell an) | 447 | Root cause: TypeScript-Fehler `Buffer<ArrayBufferLike>` not assignable to `BlobPart` in `app/api/telegram/webhook/route.ts` Zeile 196 verhinderte den Next.js-Build für diese Route — sie wurde nie auf Vercel deployt (`x-matched-path: /_not-found`). Fix: `new File([buffer], ...)` → `new File([new Uint8Array(buffer)], ...)`. Supabase Realtime (`agent_messages` in `supabase_realtime`-Publication) und Webhook-URL-Registrierung waren korrekt. Nach nächstem Vercel-Deploy (automatisch via GitHub-Merge) wird der inbound-Zeilen-Schreibpfad aktiv. |
| 2026-05-21 | Makros-Karte: Inline-Dosis-Chips + Erklärungs-Popup statt Step 3 | 441 | Step-2-Karte vereinfacht: (1) Eyebrow-Label „Angaben in {unit}" über Ring-Karten; (2) ICR-Chips: minWidth:0, overflow:hidden, whiteSpace:nowrap für sauberes Rendering; (3) eagerDoses useMemo (reine KH÷ICR+Korrektur, kein API-Call) für sofortige Dosis-Vorschau; (4) Zwei Dosis-Preview-Chips unterhalb ICR-Selector (nur bei ≥3 Samples und Differenz >0.5); (5) Aktionszeile: 1 Primary-Button (zeigt eager Dosis oder Fallback „ohne Bolus") + dezenter „Erkläre mir die Berechnung"-Link; (6) BolusExplainerSheet als `position:fixed` Bottom-Sheet (ersetzt Step 3): zeigt Dosis, Konfidenz, IOB, GPT-Reasoning, Methodik-Erklärung pro ICR-Quelle; (7) Step-3-Wizard-Seite entfernt; (8) neue i18n-Strings in de.json + en.json: carb_unit_eyebrow, btn_save_with_bolus, bolus_explainer_link, bolus_sheet_* (9 Keys). |
| 2026-05-21 | Fix Telegram webhook regex so replies actually reach the shell | 442 | Regex in `extractTaskId` (`app/api/telegram/webhook/route.ts`) von `/Task[:\s]+(\d+)/i` auf `/Task[:\s\`]+(\d+)/i` geändert — Backtick-Zeichen ergänzt, damit das Format „Task \`999\`" (wie es die ausgehende Nachricht verwendet) korrekt gematcht wird. |
| 2026-05-21 | Add a test so the Telegram reply flow never silently breaks again | 443 | `extractTaskId` in `app/api/telegram/webhook/route.ts` als `export function` exportiert. `tests/unit/telegramWebhook.test.ts` mit 7 Playwright-Unit-Tests angelegt: Backtick-Format, Colon-Format, Space-Format, eingebettet in realistischen Reply-Text — sowie drei null-Fälle (kein Match, leerer String, Task ohne Ziffern). Alle 7 Tests grün. |
| 2026-05-21 | Make the Telegram question flow testable end-to-end | 446 | `tests/unit/telegramIntegration.test.ts` angelegt: 14 Playwright-Unit-Tests decken den vollständigen Round-Trip ab. Lokaler Mock-HTTP-Server (Node `http`) simuliert die Supabase REST API (POST /rest/v1/agent_messages) — kein live Telegram Bot, kein live Supabase nötig. Tests prüfen: (1) Happy-Path Text-Reply mit Backtick-Task-ID schreibt korrekte inbound-Zeile (task_id, direction, message); (2) Secret-Validierung (falsch/fehlend → 401, Supabase nicht aufgerufen); (3) fehlende TELEGRAM_WEBHOOK_SECRET-Env → 500; (4) Voice-Duration-Guard 61 s → 413; (5) Deduplizierung: zweite identische update_id → nur ein INSERT; (6) Notify-Script-Auflösungslogik (shouldResolve-Prädikat inline nachgebaut): inbound+created_at≥sentAt → Nachricht, outbound → null, created_at<sentAt → null, Grenzwert created_at=sentAt → Nachricht. Alle 14 Tests grün in 4,8 s. |
| 2026-05-22 | IOBCard: Fix 0,0-Bug + Flip-Rückseite + futuristisches Redesign | 470 | Bug-Fix: IOBCard bezieht jetzt auch `meals.insulin_units` als Fallback-Dosen ein (Mahlzeit-Insulin wird nicht in `insulin_logs` gespiegelt → 0,0-Anzeige). Deduplizierung via `related_entry_id`-Set verhindert Doppelzählung. Flip-Animation (rotateY, 0,55s cubic-bezier, preserve-3d) analog zu CurrentDayGlucoseCard. Front: Glow-Ring via SVG drop-shadow, größere Mono-Zahl mit textShadow, inset box-shadow. Rückseite: parabolische Decay-Sparkline (grau=vergangen, farbig=verbleibend, gestrichelte Now-Linie), kompakte Dosen-Liste (Uhrzeit/Einheiten/Restmenge), DIA-Fußzeile. 6 neue i18n-Keys im dashboard-Namespace (iob_back_title, iob_dose_remaining, iob_no_active_doses, iob_dia_info, iob_dia_rapid, iob_dia_regular) in de.json + en.json. Dashboard: `meals` Prop an IOBCard weitergegeben. |
| 2026-05-22 | /welcome: Already-logged-in check + friendly duplicate-signup error | 468 | `app/welcome/page.tsx`: neuer `AuthState`-Typ ("checking"/"needs_signup"/"signed_in"), `useEffect` prüft `supabase.auth.getUser()` nach Stripe-Verifikation und zeigt "Du bist bereits angemeldet" + "Zum Dashboard →" statt Passwort-Formular. `handleSignup`: `/already/i`-Regex auf `authError.message` — bei Match `t("welcome_err_already_registered")` statt rohem Supabase-Fehler. Zwei neue i18n-Keys `welcome_err_already_registered` in `messages/de.json` + `messages/en.json`. |
| 2026-05-21 | Add a unit test to catch cache TTL bugs before they reach users | 457 | `app/(protected)/entries/cache.ts` angelegt: exportiert `ENTRIES_CACHE_KEY_PREFIX`, `ENTRIES_CACHE_TTL_MS`, `readEntriesCache` (pure Funktion, injizierbare `StorageLike`-Schnittstelle + `now`-Parameter). `entries/page.tsx` importiert daraus; inline-Logik in der Read-useEffect durch `readEntriesCache(uid, localStorage)` ersetzt. `tests/unit/entriesCache.test.ts` mit 12 Playwright-Unit-Tests: fresh cache, TTL-Grenze (exakt), expired (11 min), expired key gelöscht, missing cachedAt, string cachedAt, invalid JSON, meals kein Array, leerer Speicher (kein removeItem), uid-B liest uid-A nicht, beide UIDs unabhängig frisch, uid-A abgelaufen ohne uid-B zu löschen. Alle 12 Tests grün. |
| 2026-05-21 | Extend entries sessionStorage cache to survive full-page reloads | 452 | Entries SWR-Cache von `sessionStorage` auf `localStorage` umgestellt. Beim Schreiben wird ein `cachedAt`-Timestamp (Unix-ms) in den JSON-Payload eingebettet. Beim Lesen auf mount: fehlendes oder abgelaufenes `cachedAt` (>10 min = `ENTRIES_CACHE_TTL_MS`) → Eintrag wird gelöscht und als Cache-Miss behandelt. Cache-Key weiterhin mit User-ID. Kein D-XXX-Eintrag nötig (kein Infrastruktur- oder Schema-Wechsel). |
| 2026-05-21 | Cap the entries localStorage payload to prevent quota errors on power users | 454 | Before writing to localStorage in `loadFull` (`app/(protected)/entries/page.tsx`), the JSON payload is now size-checked against a 2 MB threshold (accurate UTF-8 bytes via TextEncoder). If it exceeds the limit, the meals array is trimmed from the tail (oldest entries, since fetchMeals returns newest-first) until it fits. All non-meal log types (insulin, exercise, cycle, symptoms, influences) and the cachedAt timestamp are preserved in full. |
| 2026-05-21 | Show a subtle 'Refreshing…' indicator when stale data is being updated | 453 | `components/RefreshingBar.tsx` angelegt: 3 px hoher, accent-farbener Balken mit CSS-Slide-Animation, verschwindet wenn frische Daten ankommen. Entries (`entries/page.tsx`): neuer `isRefreshing`-State — wird in `loadFull` auf `true` gesetzt (Beginn) und in `finally` auf `false` (Abschluss), erscheint sobald gecachte Daten gezeigt werden und die Vollabfrage noch läuft. Dashboard (`dashboard/page.tsx`): `isValidating` aus `useSWR` destrukturiert, Balken zeigt sich während SWR im Hintergrund revalidiert. Insights (`insights/page.tsx`): `isValidating` aus dem primären SWR-Hook, Balken analog. Keine neuen D-XXX-Einträge nötig (keine Infrastruktur-/Schema-/Auth-Änderung). |
| 2026-05-21 | Protect the cache write path with the same automated tests | 460 | `writeEntriesCache(uid, storage, data, now?)` + `WritableStorageLike` hinzugefügt in `app/(protected)/entries/cache.ts`. Inline `localStorage.setItem`-Block in `loadFull` (`entries/page.tsx`) durch den neuen Helper ersetzt (inkl. 2-MB-Trimm-Logik und QuotaError-Swallow). 4 neue Playwright-Unit-Tests in `tests/unit/entriesCache.test.ts`: korrekter Key (`glev:entries-cache:{uid}`), numerisches `cachedAt` gleich injiziertem `now`, alle 6 Datenarrays vorhanden, QuotaExceededError wird verschluckt und crasht die Seite nicht. Alle 16 Tests grün. |
| 2026-05-21 | Test that the size trimming actually keeps old data out when the cache is nearly full | 462 | `tests/unit/entriesCache.test.ts` um einen neuen Test erweitert: baut 220 große Meal-Objekte (~10 KB/Stück = ~2,2 MB gesamt), ruft `writeEntriesCache` auf und prüft: (1) `setItem` wurde aufgerufen, (2) geschriebenes JSON ≤ 2 MB, (3) mindestens ein Meal wurde vom Tail entfernt (älteste zuerst), (4) alle Non-Meal-Arrays (insulin, exercise, cycle, symptoms, influences) sind vollständig erhalten. Alle 17 Tests grün in 4,6 s. |
| 2026-05-22 | Fix hardcoded English strings in expanded exercise/bolus/basal/influence/symptom entry views on Entries screen | 1214860137541273 | `lib/exerciseEval.ts`: `patternNote()`, `interimMessage()`, `finalMessage()`, `personalPatternHeadline()` — `locale`-Param mit DE/EN-Inline-Übersetzungen. `lib/insulinEval.ts`: `bolusInterimMessage()`, `bolusFinalMessage()` ebenso. 23 neue i18n-Keys im `entriesExpand`-Namespace in `messages/de.json` + `messages/en.json`. `entries/page.tsx`: alle Panels (EVALUATION, POST-WORKOUT CHECK, 1H/2H OUTCOME, PATTERN NOTE, PERSONAL PATTERN, HYPO RISK SHARE, NOTES, Disclaimer, Basal-Disclaimer, Warte-Nachrichten) nutzen jetzt `tx()` aus `useTranslations("entriesExpand")`. `ExerciseRowCard`, `InsulinLogRowCard` (Bolus), `BasalRowCard`, `InfluenceRowCard`, `SymptomRowCard`, `InfluenceEditor`, `SymptomEditor` vollständig übersetzt. Ungenutzter Import `exerciseTypeLabel` entfernt. |
| 2026-05-22 | Adapt Score: Rename Control Score → Adapt Score + Formel-Fix (Pending aus Nenner) | 1215029864791639 | (1) `lib/controlScore.ts`: Nenner auf ausgewertete Mahlzeiten (GOOD+SPIKE+HYPO) beschränkt — ausstehende/OTHER-Einträge fließen nicht mehr in die Raten-Berechnung ein. `score: number \| null` zurückgegeben wenn < 3 ausgewertete Mahlzeiten. (2) Dashboard `ControlScoreCard`: `hasScore = score !== null` für die Score-Anzeige; `hasEntries = count > 0` für Badge + Aufschlüsselung; Header auf `adapt_score_title` + `adapt_score_sublabel` umgestellt. Progress-Bar und `/ 100`-Label nur bei `hasScore`. (3) i18n: 3 neue Keys (`adapt_score_title`, `adapt_score_sublabel`, `adapt_score_tooltip`) in de.json + en.json; `control_score_label`, `cluster_score_trend`, `pricing_pro_b3` auf "Adapt Score" umbenannt. (4) Alle Display-Texte mit "Control Score" in `app/mockups/dark-cockpit/page.tsx`, `components/AppMockupPhone.tsx` (tabCaption + Karten-Key) auf "Adapt Score" aktualisiert. Kein D-XXX-Eintrag nötig (kein Infrastruktur-, Schema- oder Auth-Wechsel). |
| 2026-05-21 | Keep the entries cache from storing stale data when the user logs out and back in as someone else | 461 | `clearEntriesCache(uid, storage)` in `app/(protected)/entries/cache.ts` ergänzt. In `entries/page.tsx` neuen `useEffect` mit `supabase.auth.onAuthStateChange` eingebaut: auf `SIGNED_OUT` wird die Cache-Key für die abgemeldete UID sofort gelöscht (defensive Fallback: prefix-scan, falls `session?.user?.id` null ist). 4 neue Playwright-Unit-Tests in `tests/unit/entriesCache.test.ts`: key wird gelöscht, anderer UID-Key bleibt unberührt, no-op bei fehlendem Key, readEntriesCache gibt danach null zurück. Alle 20 Tests grün. |
| 2026-05-22 | IOB history window toggle (12 h / 24 h) with localStorage persistence | 483 | `components/IOBHistoryChart.tsx`: ersetzt statisches "24 h"-Label durch Pill-Toggle (12 h / 24 h); Zustand wird in `localStorage` (`glev:iob_history_hours`) persistiert; Zeitachsen-Labels skalieren dynamisch (3 h bei 12-h-Ansicht / 6 h bei 24-h-Ansicht). |
| 2026-05-22 | Mahlzeit-loggen-Button aus Marketing-Phone entfernen und Bottom-Nav angleichen | 466 | (1) `components/AppMockupPhone.tsx` `DashboardScreen`: `onLogMeal`-Prop + dashed CTA-Button ersatzlos entfernt. (2) `BottomNav` angeglichen: Dashboard-Icon → Home/Haus-SVG (1:1 `Layout.tsx` L826); Glev-Mittel-Slot → eleviertes FAB-Bubble (44 px, 10 px lift, accent border+shadow+GlevLogo 22 px; `overflow:visible`). (3) `app/mockups/dark-cockpit/page.tsx` `MobileDashboard`: `mobileNavItems` (4 Emoji-Items) + "LOG"-Mic-FAB ersetzt durch 5-Tab-Nav 1:1 mit `Layout.tsx` — Home/Balken/[Glev-FAB 52 px]/Lightbulb/Gear als SVGs, "settings"-Tab neu, Glev-FAB → "recommend"-Page. (4) `tests/e2e/marketing-mockup.spec.ts`: `/Mahlzeit loggen/`-Assertion entfernt; Snapshot-Anker auf `Glukose · live` umgestellt. (5) `phone-dashboard-chromium-linux.png`-Snapshot neu generiert (ohne Button, mit Home-Icon). |
| 2026-05-22 | IOBCard: Erwartete BG-Wirkung auf Rückseite (IOB × KF) | 473 | `components/IOBCard.tsx`: neuen `currentBg?: number`-Prop ergänzt; `cf` via `getInsulinSettings()` (localStorage-Mirror, synchron) in `useMemo` gelesen; `expectedDrop = Math.round(iob * cf)` und `projectedBg = max(20, currentBg − expectedDrop)` berechnet. Rückseite: neuer „Erwartete BG-Wirkung"-Abschnitt (nur wenn !cleared) mit Drop-Chip (~X mg/dL in Akzentfarbe), Formel-Label (IOB IE × KF Y), optionalem Ziel-Chip (Erwartet: ~Z mg/dL, nur wenn currentBg übergeben) und Compliance-Hinweis-Fußzeile. 5 neue i18n-Keys in dashboard-Namespace (de+en): iob_bg_impact_label, iob_bg_impact_value, iob_bg_impact_formula, iob_projected_bg, iob_bg_hint. Dashboard: `currentBg={cgm7d.length > 0 ? cgm7d[cgm7d.length - 1].v : undefined}` an IOBCard weitergegeben. Compliance D-003 eingehalten — alle Texte als Orientierungswert gerahmt. |
| 2026-05-22 | Add an end-to-end test that catches the IOB 0,0 regression before it ships | 472 | `buildDoses(insulin, meals?)` aus `components/IOBCard.tsx` als Pure Function in `lib/iob.ts` extrahiert (mit minimalen Duck-Typing-Interfaces `InsulinLike` + `MealLike` — keine Supabase-Abhängigkeit). `IOBCard.tsx` nutzt jetzt `buildDoses()` statt inline useMemo-Logik. `tests/unit/iobCard.test.ts` mit 16 Playwright-Unit-Tests angelegt: (1) meal.insulin_units ohne linked log → IOB > 0 (Kern-Regression); (2) linked meal via related_entry_id wird NICHT doppelt gezählt; (3) Doses nach DIA-Ablauf → IOB = 0; Grenzfälle: null/0 insulin_units ignoriert, meal_time-Fallback auf created_at, basal logs ignoriert, mehrere gemischte Doses. Alle 16 Tests grün in 3,8 s. |
| 2026-05-22 | UI-Polish-Batch: Live-Chip, Adapt-Score mini, Logo +13%, Mono-Font +1px, Header/Footer schlanker | — | Header: paddingTop von +4px auf +1px, paddingBottom 10→7, --nav-top-height 28→24px. Footer: --nav-bottom-height 50→44px, --nav-bottom-safe halbiert (max(3px, safe*0.5)), MobileTab+Fab height 56→44px, gap/padding reduziert, Fab-Bubble-Offset −20px→−14px neu kalibriert. Logo GlevLockup size 23→26 (+13%). JetBrains-Mono-Labels in ScopeHeaderChip und Engine-Wizard-Step-Indicator 11→12px. CGM-Live-Chip: /api/cgm/source einmalig auf mount gefetcht; grüner „● Live"-Pill erscheint im Header nur auf /dashboard wenn source gesetzt. Dashboard: @media (max-width:375px) für iPhone 13 mini/SE — glev-cluster-stack gap 10px, glev-trend-card height 195px. |
| 2026-05-22 | Show IOB history chart so users can see how their active insulin changed over the day | 480 | `components/IOBHistoryChart.tsx` neu erstellt: SVG-Linienchart mit Fläche unter der Kurve, 15-min-Sampling über 24 h, Zeitachse (6-h-Intervalle), Y-Achse (0/Mitte/Max IE), farbkodierte Linie + Dot (grün/amber/orange wie IOBCard), Leer-State. Nutzt `buildDoses` + `calcSingleIOB` aus `lib/iob.ts`, respektiert DIA-Setting (rapid 180 min / regular 300 min). Dashboard `control`-Cluster: `iob-history`-Karte nach IOBCard eingefügt. 5 neue i18n-Keys im `dashboard`-Namespace (iob_history_title, iob_history_window, iob_history_empty, iob_history_interval, iob_history_current) in de.json + en.json. |
| 2026-05-22 | Hotfix: Vercel build — cgm7d not in scope in DashboardPage | — | Task #473 platzierte `currentBg={cgm7d.length > 0 ? cgm7d[cgm7d.length - 1].v : undefined}` in `DashboardPage`, aber `cgm7d` ist in `TrendChart` (einer Sub-Komponente, die ab Zeile 227 definiert ist) deklariert — außerhalb des Scopes von `DashboardPage`. Fix: neuer `latestCgmBg`-State (`useState<number | undefined>(undefined)`) in `DashboardPage` + `useEffect` der letzten 3 h über `fetchCgmSamples` auf mount holt; IOBCard `currentBg={latestCgmBg}`. Kein D-XXX-Eintrag nötig. |
| 2026-05-22 | Write automated test for meal bolus mirror → insulin_logs | 478 | `lib/meals.ts` → `saveMeal`: optionaler `_deps?: SaveMealDeps`-Parameter ergänzt, der `_insertInsulinLog` für Unit-Tests injizierbar macht (kein Einfluss auf Produktionspfad). `tests/unit/_fake-supabase-meals.ts` neu angelegt: installiiert vor Modulauswertung einen konfigurierbaren Supabase-Fake auf `globalThis._supabase`, deckt `auth.getUser()`, `meals`-Insert und No-Op für alle anderen Tabellen ab. `tests/unit/mealBolusMirror.test.ts` mit 7 Playwright-Unit-Tests: (1) `insulinUnits > 0` → `insertInsulinLog` mit `insulin_type:"bolus"`, korrektem `units`, `related_entry_id` und `insulin_name:"Mahlzeit-Bolus"`; (2) `insulinUnits = 0` → kein Aufruf; (3) `insulinUnits = null` → kein Aufruf; (4) `at = mealTime` wenn vorhanden; (5) `at = createdAt` als Fallback; (6) `glucoseBefore` → `cgm_glucose_at_log`; (7) werfende `insertInsulinLog` schlägt nie den Meal-Save fehl. Alle 7 Tests grün. |
| 2026-05-22 | Let users switch the IOB history window between 12 h and 24 h | 483 | `components/IOBHistoryChart.tsx`: `hours` prop removed; internal `WindowHours` state (12\|24) added, seeded from `localStorage` key `glev:iob_history_hours` and written back on toggle. Pill toggle (12 h / 24 h) replaces the static window label in the card header — active pill gets accent background, inactive is ghost text. Chart + time-label density update instantly on click. No new i18n keys needed (labels are numeric, universally understood). |
| 2026-05-22 | Show IOB peaks as labeled markers on the history chart | 485 | `components/IOBHistoryChart.tsx`: Local-maximum detection added — iterates `samples[]` and flags index `i` as a peak when `iob[i] > iob[i-1]`, `iob[i] > iob[i+1]`, and the rise above the lower neighbor ≥ 0.5 IE. Top-3 peaks sorted by descending IOB value, capped to avoid clutter. Each peak renders inside a `<g>`: colored dot (3 px, accent-bordered), vertical stem line, and a labeled pill (46×16 px rounded rect with border + `iob_peak_label` translation text in monospace). Pill x-position is clamped within chart bounds to prevent edge clipping. New i18n key `iob_peak_label` added in both `messages/de.json` ("{time} · {units} IE") and `messages/en.json` ("{time} · {units} u"). No schema or infrastructure change; no D-XXX entry required. |
| 2026-05-22 | Mirror meal insulin doses into insulin_logs when a meal is saved | 471 | `lib/meals.ts` → `saveMeal`: nach erfolgreichem Meal-Insert wird, wenn `insulinUnits > 0`, ein fire-and-forget Bolus-Row in `insulin_logs` geschrieben (`related_entry_id = meal.id`, `insulin_name = "Mahlzeit-Bolus"`, `cgm_glucose_at_log = glucoseBefore`, `at = mealTime ?? createdAt`). Die Deduplizierung in IOBCard via `related_entry_id`-Set verhindert Doppelzählung. Import `insertInsulinLog` direkt (kein Dynamic Import, kein Circular Dependency). Mirror-Fehler werden bewusst geschluckt — der Meal-Save schlägt nie wegen der Spiegelung fehl. `app/api/log/route.ts`: erklärender Kommentar ergänzt, warum die Spiegelung dort nicht implementiert ist (Legacy-Tabelle `logs` statt `meals`; kein User-Auth-Context auf der Server-Route). |
| 2026-05-22 | Catch a wrong dose aggregation before it reaches users — cover IOB with more edge cases | 479 | `lib/iob.ts` `calcSingleIOB`: `elapsedMin <= 0` geändert auf `elapsedMin < 0` — Future-Dosen (meal_time in der Zukunft) gaben bisher fälschlicherweise `dose.units` zurück statt 0. `tests/unit/iobCard.test.ts`: 11 neue Edge-Case-Tests in drei Gruppen: (4) Basal-Log MIT related_entry_id wird weiterhin aus IOB ausgeschlossen + Behavior-Lock für basal-shadowing-Wechselwirkung; (5) Future meal_time trägt nicht zum IOB bei — calcSingleIOB einzeln + calcTotalIOB + Mix aus Past+Future; (6) Präzision/Rounding nahe 0.05-Threshold — calcTotalIOB rundet auf 2 Dezimalstellen, formatIOBDisplay(0.04)→null, formatIOBDisplay(0.05)→"0.1 IE", formatIOBDisplay(0.049)→null, near-zero-Residual-Lock. Kein D-XXX-Eintrag nötig (kein Infrastruktur-/Schema-/Auth-Wechsel). |
| 2026-05-22 | Tap a peak marker to see which meal or dose caused that IOB spike | 490 | `lib/iob.ts`: `InsulinLike` erweitert um optionales `insulin_name`; `MealLike` erweitert um optionales `input_text`; `BolusDose` erweitert um `source?: 'insulin' \| 'meal'` und `label?: string`; `buildDoses` befüllt diese Felder aus den Quelldaten (Mahlzeit-Labels auf 30 Zeichen gekürzt); neue Pure-Function `getActiveDosesAtTime(doses, tMs, diaMin)` gibt alle Dosen zurück, die zu einem gegebenen Zeitpunkt noch IOB beitragen. `components/IOBHistoryChart.tsx`: Peak-Marker-Gruppen sind jetzt tappable (`cursor:pointer`, `onClick`+`stopPropagation`, `role="button"`); breites unsichtbares Tref-Rechteck erhöht Touch-Genauigkeit; `activePeakIdx`-State tracked welcher Peak offen ist; beim Tap öffnet sich ein `foreignObject`-Popover in SVG-Koordinaten (Dosis-Zeitpunkt, Quellname, Einheiten, Akzentfarbe); Pill-Hintergrund wechselt bei aktivem Peak auf Vollfarbe für visuelles Feedback; Schließen via SVG-Klick außerhalb oder `document.mousedown`/`touchstart` für Klicks außerhalb der Komponente. 3 neue i18n-Keys im `dashboard`-Namespace: `iob_peak_popover_title`, `iob_peak_popover_meal_label`, `iob_peak_dose_units` in de.json + en.json. Kein D-XXX-Eintrag nötig. |
| 2026-05-22 | Warn users when a recommended dose vanishes due to rounding so nothing is silently lost | 508 | `lib/iob.ts`: neue Pure-Function `iobCorrectionRoundedToZero(recommendation, iob)` exportiert — gibt `true` wenn `recommendation − iob > 0` aber `applyIOBCorrection` 0 zurückgibt (Rundungs-Fall, nicht Math.max-Clamp). `app/(protected)/engine/page.tsx`: Import um `iobCorrectionRoundedToZero` erweitert; bestehende "Kein zusätzliches Insulin nötig"-Badge-Bedingung von `iob > 0 && applyIOBCorrection === 0` auf `iob > 0 && result.dose <= iob` präzisiert (schließt nun korrekt nur den IOB-Covers-Recommendation-Fall ein); neue blaue Info-Badge `ℹ {tEngine("dose_rounded_to_zero")}` erscheint wenn `iobCorrectionRoundedToZero` true ist. `messages/de.json` + `messages/en.json`: neuer Key `dose_rounded_to_zero` im `engine`-Namespace ("Empfehlung < 0,1 IE — kein Bolus nötig" / "Recommendation < 0.1 u — no bolus needed"). `tests/unit/iobCorrection.test.ts`: 7 neue Unit-Tests für `iobCorrectionRoundedToZero` (Gruppe 6) — 2 true-Fälle (tiny dose no IOB, tiny remainder with IOB) + 5 false-Fälle (clamp, equal, displayable, rounds-up, both-zero). Gesamt 24 Tests grün in 4,1 s. Kein D-XXX-Eintrag nötig. |
| 2026-05-22 | Catch a wrong correction silently passing through when recommendation rounds to zero | 506 | `tests/unit/iobCorrection.test.ts` um 4 Grenzfall-Tests erweitert (neue Gruppe 5 „Near-zero boundary"): (1) recommendation=0.05, iob=0 → 0.1 (rundet auf, wird nicht still zu 0); (2) recommendation=0.04, iob=0 → 0 (rundet korrekt ab); (3) recommendation=0.09, iob=0.05 → result=0.04 → 0.0 (durch Rundung, nicht durch Math.max-Clamp); (4) recommendation=0.15, iob=0.05 → 0.1 (sauberer Fall nahe Threshold). Alle 17 Tests grün in 4,7 s. Kein D-XXX-Eintrag nötig (keine Infrastruktur-/Schema-/Auth-Änderung). |
| 2026-05-22 | Add automated tests for the calcTotalIOB correction that subtracts active insulin from a recommendation | 502 | `tests/unit/iobCorrection.test.ts` angelegt mit 13 Playwright-Unit-Tests für `applyIOBCorrection(recommendation, iob)` in `lib/iob.ts`. Vier Invarianten abgedeckt: (1) Normalfall: recommendation − iob korrekt subtrahiert (ganzzahlig + gebrochen); (2) Clamp: Ergebnis ist nie negativ — iob ≥ recommendation → 0; iob stark überschreitend → 0; recommendation = 0 → 0; (3) Rundung auf 1 Dezimalstelle (auf + ab); (4) iob = 0 lässt recommendation unverändert (ganzzahlig, gebrochen, beides = 0). Alle 13 Tests grün in 4,9 s. Kein D-XXX-Eintrag nötig (keine Infrastruktur-/Schema-/Auth-Änderung). |
| 2026-05-22 | Show users which meal caused each IOB peak so they can learn their patterns | 495 | Feature war durch Task #490 bereits vollständig implementiert (`getActiveDosesAtTime`, `activePeakIdx`-State, `foreignObject`-Popover mit Mahlzeit-Name/Dosis-Label/Einheiten). Einzig fehlend: hardcodierter Fallback-String `"Bolus"` für manuelle Injektionen ohne `insulin_name`. Fix: neuer i18n-Key `iob_peak_manual_bolus` in `messages/de.json` ("Manueller Bolus") und `messages/en.json` ("Manual bolus") ergänzt; `IOBHistoryChart.tsx` nutzt jetzt `t("iob_peak_manual_bolus")` statt dem hardcodierten String. |
| 2026-05-22 | Add automated tests to verify the peak-to-meal link works correctly | 498 | `lib/iob.ts` `buildDoses`: Bolus-Log ohne `insulin_name` fällt jetzt auf `"Manual bolus"` zurück (statt `undefined`). `getActiveDosesAtTime` aus `lib/iob.ts` importiert in `tests/unit/iobCard.test.ts`. 14 neue Playwright-Unit-Tests in drei Gruppen: (7) `getActiveDosesAtTime` — aktive Dosis innerhalb DIA → enthalten; vollständig verfallene Dosis (elapsed ≥ DIA) → ausgeschlossen; Dosis genau an der DIA-Grenze → ausgeschlossen; Zukunfts-Dosis (elapsed < 0) → ausgeschlossen; gemischte Active+Expired+Future → nur aktive zurückgegeben; leere Liste → leeres Array. (8) Label-Truncation — 30 Zeichen → unverändert; 31 Zeichen → 28 Zeichen + "…"; langer Text → slice(0,28)+"…"; kurzer Text verbatim; kein input_text → undefined. (9) Manual-Bolus-Fallback — kein `insulin_name` → `"Manual bolus"`; mit `insulin_name` → Name; mehrere Bolus-Logs mit und ohne Name → je korrekt. Gesamt 41 Tests grün in 3,7 s. |
| 2026-05-22 | Add automated tests for the IOB history sampling logic | 484 | `tests/unit/iobHistory.test.ts` angelegt mit 10 Playwright-Unit-Tests für `buildIOBHistory` (lib/iob.ts): (1) Alle Samples vor einer Dosis haben IOB = 0; (2) Dosis nach Sample-Zeitpunkt trägt 0 bei; (3) Peak liegt nahe dem Injektionszeitpunkt; (4) IOB = 0 nach DIA; (5) Aktive Dosis > 0 am aktuellen Zeitpunkt; (6) Zwei gestapelte Dosen — gemeinsamer Peak > jede Einzel-Dosis; (7) Leeres Dosen-Array → alle Samples = 0; (8) Rapid (180 min) cleared vor Regular (300 min); (9) Sample-Count = (hours × 60 / intervalMin) + 1; (10) Monotoner Abfall nach Peak-Zeitpunkt. `buildIOBHistory` war bereits in lib/iob.ts exportiert (Task #480). Kein neuer D-XXX-Eintrag nötig. |
| 2026-05-22 | Catch missing translation keys on the Dashboard and Insights pages too | 514 | `tests/unit/dashboardTranslationKeys.test.ts`: extrahiert alle statischen `t("…")`-Literale (Wortgrenze-Regex `\bt\(`) aus `app/(protected)/dashboard/page.tsx` (Dashboard-Übersetzer heißt `t`, nicht `tDashboard`). Zusätzlich explizit `badge_strong/good/poor` als dynamische Keys (Template-Literal `t(\`badge_\${badge.key}\`)`). Namespace-Parity-Check de ↔ en + Non-empty-Guard. `tests/unit/insightsTranslationKeys.test.ts`: statische `tInsights("…")`-Literale + 9 dynamische Key-Gruppen explizit: `exercise_type_*` (14 Typen), `workout_outcome_*` + `workout_outcome_label_*` (4 Outcomes), `workout_tod_*` (4), `workout_dur_*` (3), `pattern_*_label/explanation` (5 Pattern-Typen), `cycle_phase_*` (6), `symptom_*` (17 Symptom-Keys). Beide Tests: 4 Checks je Datei (Non-empty, de.json-Präsenz, en.json-Präsenz, Parity). Alle 8 Tests grün. Bonus: Insights-Test deckte 8 fehlende `pattern_*`-Keys in `messages/en.json` auf → sofort ergänzt (`pattern_overdosing_label/explanation`, `pattern_underdosing_label/explanation`, `pattern_spiking_label/explanation`, `pattern_insufficient_data_label/explanation`). |
| 2026-05-22 | Add automated tests to catch missing Engine translation keys before users see them | 512 | `tests/unit/engineTranslationKeys.test.ts` angelegt mit 3 Playwright-Unit-Tests: (1) alle statischen `tEngine("…")`-Keys aus `engine/page.tsx` sind in `messages/de.json` vorhanden; (2) alle Keys sind in `messages/en.json` vorhanden; (3) beide Namespaces haben exakt dieselben Keys (keine Locale-Drift). Keys werden per Regex aus der Quell-Datei extrahiert — kein Mocking, kein Server nötig. 6 dynamische Konfidenz-Keys (`conf_label_HIGH/MEDIUM/LOW`, `conf_explain_HIGH/MEDIUM/LOW`) explizit ergänzt, da Template-Literal-Calls nicht vom Regex erfasst werden. Test hat außerdem 7 fehlende `adjustment_*`-Keys aufgedeckt (`adjustment_banner_title`, `adjustment_apply`, `adjustment_apply_busy`, `adjustment_apply_failed`, `adjustment_applied_toast`, `adjustment_dismiss`, `adjustment_dismiss_hint`) — diese wurden sofort in beide Locale-Dateien ergänzt. Alle 3 Tests grün. |
| 2026-05-22 | Fix the adjustment banner copy in German — current wording sounds robotic | 515 | All 7 `adjustment_*` keys in the `engine` namespace reviewed and polished. German: `adjustment_banner_title` "ICR-Anpassung vorgeschlagen" → "Update für deinen KH-Faktor" (drops clinical abbreviation, uses in-app term "KH-Faktor"); `adjustment_apply` "Übernehmen" → "Jetzt aktualisieren" (warmer CTA); `adjustment_apply_busy` "Wird angepasst …" → "Wird gespeichert …" (consistent with other busy states); `adjustment_apply_failed` "Anpassung fehlgeschlagen" → "Nicht gespeichert – bitte erneut tippen" (actionable); `adjustment_applied_toast` "ICR wurde angepasst" → "KH-Faktor aktualisiert" (user-friendly term); `adjustment_dismiss` "Später" unchanged (already natural); `adjustment_dismiss_hint` "Erinnerung in 14 Tagen" → "In 14 Tagen wieder anzeigen" (sentence rather than label). English counterparts updated for consistency: banner title → "Carb ratio update available", apply → "Update now", busy → "Saving …", failed → "Couldn't save — please try again", toast → "Carb ratio updated", dismiss_hint → "We'll remind you in 14 days". No schema or infrastructure change. |
| 2026-05-22 | Localise the existing IOB badge messages so they follow the language picker | 511 | Two hardcoded German strings in `app/(protected)/engine/page.tsx` (IOB active badge and IOB covers recommendation badge) replaced with `tEngine("iob_active", { iobDisplay })` and `tEngine("iob_covers_dose")`. New keys `iob_active` and `iob_covers_dose` added to the `engine` namespace in `messages/de.json` ("⚡ Noch aktiv: {iobDisplay} — Empfehlung bereits bereinigt" / "✓ Kein zusätzliches Insulin nötig — bestehende Wirkung reicht aus") and `messages/en.json` ("⚡ Still active: {iobDisplay} — recommendation already adjusted" / "✓ No additional insulin needed — existing effect is sufficient"). No visual change for German users. English-locale users now see localised copy. |
| 2026-05-22 | Use English labels for the three dashboard rate chips | 518 | `messages/de.json`: `dashboard.good_label` "Treffer-Quote" → "Good Rate", `dashboard.spike_label` "Spike-Quote" → "Spike Rate", `dashboard.hypo_label` "Hypo-Quote" → "Hypo Rate". English locale already had the correct values. No code changes needed — the chips read from i18n keys; swapping the German strings to the English equivalents fixes truncation in both locales simultaneously. |
| 2026-05-21 | Faster tab navigation: skeleton with logo, stale-while-revalidate cache, trimmed queries | 451 | (1) `app/(protected)/entries/loading.tsx` neu angelegt mit Glev-Logo + 5 Skeleton-Blöcken (Filterzeile + Kartenstapel). (2) Glev-Logo (InlineLogo als inlined SVG — kein Client-Bundle-Overhead) in `dashboard/loading.tsx`, `insights/loading.tsx` und `engine/loading.tsx` nachgezogen — konsistentes visuelles Muster auf allen Tabs. (3) Stale-While-Revalidate für Entries: neuer `useEffect` in `entries/page.tsx` liest `glev:entries-cache:{userId}` aus `sessionStorage` auf mount und setzt alle 6 Datensätze sofort (kein Spinner beim Wiederbesuch). Nach jedem `loadFull` wird der Cache mit den frischen Daten überschrieben. Cache-Key enthält User-ID gegen Account-Crossover. Dashboard und Insights haben bereits SWR via `swr`-Bibliothek + `SWRProvider` — kein weiteres Caching nötig. (4) `next.config.ts`: `experimental.staleTimes = { dynamic: 60, static: 300 }` gesetzt — Next.js Router Cache hält Tabs 60 s vor, Rück-Navigationen kommen sofort aus dem Memory-Cache. (5) Initiale Meals-Query in `loadFull` von 365 auf 90 Tage getrimmt (`fetchMeals({ sinceDays: 90 })`); danach background-Fetch für das volle Fenster (`FETCH_MEALS_DEFAULT_SINCE_DAYS`), der ältere Rows in `setMeals` merged sobald er fertig ist. Kein neuer D-XXX-Eintrag: `next.config.*`-Änderung ist eine Performance-Tuning-Einstellung ohne Plattform- oder Infrastrukturwechsel. |
