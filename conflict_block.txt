| 2026-06-04 | AI Chat: Sport-Logging via Mini Preview (Task #1159) | 1159 | **`components/EngineLogTab.tsx`**: `ExerciseForm` `useEffect` erweitert. Bisher: nur `glev:open-exercise-log`-Event, nur `exercise_type`/`duration_minutes`/`intensity`. Jetzt: (1) On-mount sessionStorage-Read von `"glev_pending_exercise"` — übernimmt Payload, den `navigateToLogScreen` vor der Navigation geschrieben hat, sodass Prefill auch nach einem Full-Page-Reload greift. Key wird nach dem Lesen gelöscht. (2) Event-Handler erweitert um `notes` (setzt `setNotes`) und `logged_at` (konvertiert ISO → `datetime-local`, setzt `setCustomStartAt` + `setStartedMinAgo(STARTED_CUSTOM)` → öffnet Custom-Zeitpunkt-Picker mit vorausgefülltem Wert). Beide Pfade (mount + event) rufen dieselbe lokale `applyPrefill`-Funktion. Alle anderen Teile der Feature waren bereits implementiert: `log_exercise_entry`-AI-Tool in `glevTools.ts`, `ExerciseEntryPayload` + NAV_CONFIG in `pendingActions.ts` (tab: "log", event: "glev:open-exercise-log", storageKey: "glev_pending_exercise"), PendingActionWidget [Schnell speichern] + [Detail →] in `GlevAIChatSheet.tsx`, `navigateToLogScreen` in `useGlevAI.ts`, `execLogExerciseEntry` in `confirm-action/route.ts`. Kein neuer D-XXX-Eintrag nötig (kein neuer Cloud-Service, kein Schema-Change, kein Auth-/Compliance-Prinzip geändert — reine Client-seitige Prefill-Erweiterung). |
<<<<<<< HEAD
| 2026-06-04 | AI Chat: Einfluss-Logging (Alkohol, Stress, etc.) via Mini Preview (Task #1163) | 1163 | **Three-layer change.** (1) **New influence types (DB + lib + AI tool):** `supabase/migrations/20260604_influence_types_extend.sql` drops and re-creates the `influence_logs.influence_type` CHECK constraint to add `stress`, `illness`, `sleep_deprivation` alongside the existing `alcohol`, `cannabis`, `medication`, `other`. `lib/influences.ts` INFLUENCE_TYPES array updated (kept `cannabis` for backward compat with existing rows). `lib/ai/glevTools.ts`: tool enum extended to 7 types, tool description updated to explicitly name Stress/Erkrankung/Schlafmangel triggers, executor validation and `typeLabel` map updated, summary format changed from `"Einfluss: Label (details) — amount um HH:MM"` to the cleaner chip format `"Label · amount · HH:MM"`. `app/api/ai/confirm-action/route.ts`: same 7-type validation list updated. (2) **Nav config fix:** `lib/ai/pendingActions.ts` `log_influence_entry` NAV_CONFIG tab changed from `"log"` to `"influences"` so the Detail button navigates to `/engine?tab=influences` instead of the generic log tab. Also updated `InfluenceEntryPayload` to carry `details` and `amount` fields (was using `intensity` which the tool doesn't actually send). (3) **Form pre-population:** `components/InfluenceLogForm.tsx` gains a `useEffect` that (a) reads `sessionStorage["glev_pending_influence"]` on mount and (b) listens for `glev:open-influence-log` DOM events. `applyPendingInfluence()` parses the payload and calls setters for `influenceType`, `details`, `amount`, `notes`; time is mapped: within ±2 min of now → quickAgo=0 (Jetzt chip), otherwise → quickAgo=QUICK_CUSTOM + sets `occurredAt` to local datetime-local string. `messages/de.json` + `messages/en.json`: 3 new i18n keys each (`influence_type_stress`, `influence_type_illness`, `influence_type_sleep_deprivation`). Kein D-XXX-Eintrag nötig (kein neuer Cloud-Service, keine nicht-rückwärtskompatible Schema-Änderung — reine Client-seitige Prefill-Erweiterung + UI-Konsistenz). |
| 2026-06-04 | Fix broken e2e login flow so all Playwright tests can run in Replit dev (Task #1157) | 1157 | **Root cause:** `router.refresh()` + `router.replace("/dashboard")` in `app/login/page.tsx` used Next.js client-side routing, which Playwright's `waitForURL` hook does not reliably track in the Replit dev environment (headless Chromium with Turbopack). **Fix 1 (primary):** `app/login/page.tsx` — replaced `router.refresh()` + `router.replace("/dashboard")` with `window.location.replace("/dashboard")` after successful `signInWithPassword`. A hard browser navigation sends all auth cookies to the server, the middleware sees the valid session, and Playwright's `waitForURL(/\/dashboard/)/)` fires reliably. **Fix 2 (secondary):** Created `tests/support/login.ts` — shared `ensureLoggedIn(page, workerIndex)` helper that (a) pre-sets `glev_cookie_consent` via `addInitScript` so the CookieBanner never overlays the login form after `context.clearCookies()`, (b) navigates to /dashboard first and only logs in if redirected, (c) dismisses BzCheckModal via the focus+Escape strategy. **Updated specs:** `tests/e2e/mic-button-hold-to-talk.spec.ts` removed local `loginAsTestUser` and uses shared helper; `tests/e2e/voice-intent-bolus.spec.ts` removed local `ensureLoggedIn` and imports from shared helper; `tests/e2e/sensor-alarme-sheets.spec.ts` replaced 14 `loginAsTestUser` calls with `ensureLoggedIn` from shared helper. Kein neuer D-XXX-Eintrag nötig (kein neuer Cloud-Service, kein Schema-Change, kein Auth-/Compliance-Prinzip geändert — reine Bugfix in Login-Redirect + Test-Infrastruktur). |
=======
| 2026-06-04 | AI Chat: Einfluss-Logging (Alkohol, Stress, etc.) via Mini Preview (Task #1163) | 1163 | **Three-layer change.** (1) **New influence types (DB + lib + AI tool):** `supabase/migrations/20260604_influence_types_extend.sql` drops and re-creates the `influence_logs.influence_type` CHECK constraint to add `stress`, `illness`, `sleep_deprivation` alongside the existing `alcohol`, `cannabis`, `medication`, `other`. `lib/influences.ts` INFLUENCE_TYPES array updated (kept `cannabis` for backward compat with existing rows). `lib/ai/glevTools.ts`: tool enum extended to 7 types, tool description updated to explicitly name Stress/Erkrankung/Schlafmangel triggers, executor validation and `typeLabel` map updated, summary format changed from `"Einfluss: Label (details) — amount um HH:MM"` to the cleaner chip format `"Label · amount · HH:MM"`. `app/api/ai/confirm-action/route.ts`: same 7-type validation list updated. (2) **Nav config fix:** `lib/ai/pendingActions.ts` `log_influence_entry` NAV_CONFIG tab changed from `"log"` to `"influences"` so the Detail button navigates to `/engine?tab=influences` instead of the generic log tab. Also updated `InfluenceEntryPayload` to carry `details` and `amount` fields (was using `intensity` which the tool doesn't actually send). (3) **Form pre-population:** `components/InfluenceLogForm.tsx` gains a `useEffect` that (a) reads `sessionStorage["glev_pending_influence"]` on mount and (b) listens for `glev:open-influence-log` DOM events. `applyPendingInfluence()` parses the payload and calls setters for `influenceType`, `details`, `amount`, `notes`; time is mapped: within ±2 min of now → quickAgo=0 (Jetzt chip), otherwise → quickAgo=QUICK_CUSTOM + sets `occurredAt` to local datetime-local string. `messages/de.json` + `messages/en.json`: 3 new i18n keys each (`influence_type_stress`, `influence_type_illness`, `influence_type_sleep_deprivation`). Kein D-XXX-Eintrag nötig (kein neuer Cloud-Service, keine nicht-rückwärtskompatible Schema-Änderung — reine Erweiterung, kein Auth-/Compliance-Prinzip geändert). |
| 2026-06-04 | Add unified log_insulin AI chat tool with brand-to-type inference and rich chip UI (Task #1161) | 1161 | **`lib/ai/glevTools.ts`**: New `log_insulin` tool added to `GLEV_TOOLS` (before `navigate_to`) with inline brand→type lookup table in description (Bolus: Fiasp/NovoRapid/Humalog/Apidra/Lyumjev/Novolog/Admelog; Basal: Tresiba/Degludec/Lantus/Glargine/Toujeo/Basaglar/Levemir/Detemir). `GlevToolName` union extended with `"log_insulin"`. `PendingActionEnvelope` extended with optional `payload?: Record<string,unknown>` (only set for `log_insulin`). `createPendingAction()` gains optional `clientPayload?` parameter — included in envelope only when provided. New `toolLogInsulinEntry()` validates units (0-100 IE), infers `bolus`/`basal` from `insulin_type`, builds summary, and calls `createPendingAction` with `params` as both server params and `clientPayload`. **`lib/ai/pendingActions.ts`**: New `InsulinEntryPayload` type. `PendingActionKind` union extended with `"log_insulin"`. `NAV_CONFIG` entry: `{ tab: "bolus", event: "glev:open-bolus-log", storageKey: "glev_pending_insulin" }`. `getActionMeta` case: `{ icon: "💉", label: "Insulin" }`. **`lib/useGlevAI.ts`**: `PendingAction` extended with `payload?: Record<string,unknown>`. SSE parser: `pending_action` frame typed to include optional `payload`; spread into chip state when present. **`components/GlevAIChatSheet.tsx`**: `onDetailOpen` call now passes `pa.payload` as 4th arg. New `log_insulin` branch in `PendingActionWidget` (before generic non-meal branch): mini-preview row (brand · units IE · Bolus/Basal badge with type-colored pill · relative time), [Schnell speichern] button, [Bolus-Details →/Basal-Details →] button, ✕ dismiss top-right. Badge colors: bolus=#4F6EF7, basal=#10b981. **`components/EngineLogTab.tsx`**: New mount `useEffect` reads `sessionStorage["glev_pending_insulin"]`, removes key, pre-fills `units`, `insulin_name`, `insulin_type` (calls `handleTypeChange`), `notes`, and computes `takenMinAgo`/`customTakenAt` from `logged_at` (≤0→now, ≤5→5min, ≤15→15min, else→TAKEN_CUSTOM with `toLocalDtString`). Existing `glev:open-bolus-log` handler extended to handle `insulin_type` and `logged_at` from the event detail (same time logic). **`app/api/ai/confirm-action/route.ts`**: `log_insulin` case dispatches to `execLogBasalEntry` when `params.insulin_type === "basal"`, else `execLogBolusEntry`. Kein neuer D-XXX-Eintrag nötig (kein neuer Cloud-Service, kein Schema-Change, kein Auth-/Compliance-Prinzip geändert — `log_insulin` nutzt dieselbe `insulin_logs`-Tabelle wie die bestehenden `log_bolus_entry`/`log_basal_entry`-Tools). |
| 2026-06-04 | Fix broken e2e login flow so all Playwright tests can run in Replit dev (Task #1157) | 1157 | **Root cause:** `router.refresh()` + `router.replace("/dashboard")` in `app/login/page.tsx` used Next.js client-side routing, which Playwright's `waitForURL` hook does not reliably track in the Replit dev environment (headless Chromium with Turbopack). **Fix 1 (primary):** `app/login/page.tsx` — replaced `router.refresh()` + `router.replace("/dashboard")` with `window.location.replace("/dashboard")` after successful `signInWithPassword`. A hard browser navigation sends all auth cookies to the server, the middleware sees the valid session, and Playwright's `waitForURL(/\/dashboard/)` fires reliably. **Fix 2 (secondary):** Created `tests/support/login.ts` — shared `ensureLoggedIn(page, workerIndex)` helper that (a) pre-sets `glev_cookie_consent` via `addInitScript` so the CookieBanner never overlays the login form after `context.clearCookies()`, (b) navigates to /dashboard first and only logs in if redirected, (c) dismisses BzCheckModal via the focus+Escape strategy. **Updated specs:** `tests/e2e/mic-button-hold-to-talk.spec.ts` removed local `loginAsTestUser` and uses shared helper; `tests/e2e/voice-intent-bolus.spec.ts` removed local `ensureLoggedIn` and imports from shared helper; `tests/e2e/sensor-alarme-sheets.spec.ts` replaced 14 `loginAsTestUser` calls with `ensureLoggedIn` from shared helper. Kein neuer D-XXX-Eintrag nötig (kein neuer Cloud-Service, kein Schema-Change, kein Auth-/Compliance-Prinzip geändert — reine Bugfix in Login-Redirect + Test-Infrastruktur). |
>>>>>>> 78af9cd (feat(ai): add unified log_insulin tool with brand→type inference and rich chip UI (Task #1161))
| 2026-06-04 | Show queued meal count in chip so users know more meals are waiting (Task #1151) | 1151 | **`components/GlevAIChatSheet.tsx`**: (1) `PendingActionWidget` erhält zwei neue optionale Props: `mealChipIndex?: number` (1-basierte Position unter unerledigten Mahlzeit-Chips) und `mealChipTotal?: number` (Gesamtzahl unerledigter Mahlzeit-Chips im Turn). (2) Im inaktiven Mahlzeit-Chip-Layout: neues `showQueueBadge`-Flag (`inactive && mealChipIndex != null && mealChipTotal != null && mealChipTotal > 1`); wenn true → kleines Pill-Badge „Mahlzeit N von M" (11 px, `var(--text-muted)`, `var(--border-soft)` Hintergrund, Border-Radius 10). Badge verschwindet, sobald alle vorherigen Chips aufgelöst sind (Chip wird aktiv → `inactive = false` → `showQueueBadge = false`). (3) Render-Loop: Pre-Pass vor dem `.map()` zählt `totalUnresolvedMeals` mit `.filter()`; separater `mealChipPosition`-Counter trackt 1-basierte Position jedes Chips unter den unerledigten. `mealChipTotal` wird nur übergeben wenn `> 1` (Badge erscheint nicht bei Einzel-Mahlzeit-Turn). Kein D-XXX-Eintrag nötig (kein neuer Cloud-Service, kein Schema-Change, kein Auth-/Compliance-Prinzip geändert — reine UI-Ergänzung). |
| 2026-06-04 | AI Chat Non-Meal Log: Shared infrastructure (PendingActionWidget + Quick Save) (Task #1158) | 1158 | **`lib/ai/pendingActions.ts`** (new): Discriminated-union payload types for all log kinds (`MealEntryPayload`, `ExerciseEntryPayload`, `SymptomEntryPayload`, `InsulinBolusdPayload`, `InsulinBasalPayload`, `FingerstickPayload`, `InfluenceEntryPayload`, `CycleEntryPayload`, `AppointmentPayload`, `TimelineCheckPayload`, `UpdateSettingPayload`, `SaveObservationPayload`). `PendingActionKind` union + `NonMealActionKind` derived type. `ActionNavConfig` per-kind mapping (`tab`, `event`, `storageKey`). `getActionNavConfig(kind)` returns config for non-meal kinds, null for meal. `getActionMeta(kind)` returns emoji icon + human label per kind. **`lib/useGlevAI.ts`**: (1) Import `getActionNavConfig`. (2) New `quickSaveAction(messageId, token)` — delegates to existing `confirmAction`, marks chip "Gespeichert ✓" without navigating. (3) New `navigateToLogScreen(messageId, token, kind, payload?)` — looks up `ActionNavConfig`, writes payload to sessionStorage under `config.storageKey`, dispatches typed DOM event (`config.event`), marks chip cancelled locally, then calls `onNavigate(/engine?tab=<tab>)`. Both exported in the hook return. **`components/GlevAIChatSheet.tsx`**: (1) Import `getActionMeta`. (2) `Props` extended with `onQuickSaveAction?` + `onDetailOpen?`. (3) `PendingActionWidget` signature extended with `onQuickSave?` + `onDetailOpen?`. (4) Non-meal chip layout replaced: ✕ dismiss top-right + type icon/label header row + summary + [Schnell speichern] [Detail →] buttons. Meal chip layout unchanged. Render-loop passes `onQuickSave` + `onDetailOpen` per chip. **`components/Layout.tsx`**: Both `GlevAIChatSheet` instances (sheet + fullscreen) receive `onQuickSaveAction={glevAi.quickSaveAction}` + `onDetailOpen={glevAi.navigateToLogScreen}`. Kein D-XXX-Eintrag nötig (kein neuer Cloud-Service, kein Schema-Change, kein Auth-/Compliance-Prinzip geändert — reine Client-seitige Infrastruktur-Feature). |
